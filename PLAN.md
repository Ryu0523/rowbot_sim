# USV RL+MPC in SS4–5 — 技术方案

> 目标：在 SS4–5 海况下，用 RL 调参的 MPC 控制 USV，扩大作业窗口。
> 核心判断：需要**耐波性级别的物理** + **控制学习的工具**，现有工具没有一个横跨两者。

---

## 0. 方案定型

| 项 | 选择 | 理由 |
|---|---|---|
| 运动方程 | **非线性 Cummins** | 小 USV 在 SS5 下 Hs/吃水 ≈ 4，线性理论不够 |
| 水动力系数 | **Capytaine (BEM)** | 开源、Python、从真实船体几何算 |
| 辐射记忆 | **状态空间实现**（2–6 阶） | 卷积无法进 MPC，必须转成 ODE |
| 粘性阻尼 | Phase 1 用势流；Phase 2 起补 | 势流对垂荡/纵摇够用，只有横摇失效 |
| 仿真自由度 | Phase 1: 3 → **Phase 2 起 6** | BEM 本来就给 6×6；舵-横摇-艏摇强耦合不能拆 |
| MPC 内部模型 | 3–4 DOF 降阶 | 仿真高保真、控制器降阶是标准做法 |
| 参照船型 | 球体验码 → **10 m Wigley** | 5415 在错误的物理区间（λ/L≈1 vs 15） |
| 控制架构 | **RL 调 MPC 权重** + 学终端代价 | 唯一能在真船上收尾的架构 |
| RL 预算 | **10⁵–10⁶ 步** | 十个权重，不是从零训神经网络 |

**速度预算**：非线性 Cummins 约 10–100× 实时 → 10⁶ 步 ≈ 3 小时。可接受。

---

## 1. 为什么小 USV 和大船不是一个问题

SS5：Hs = 3.25 m，Tp = 9.7 s，λp ≈ 147 m

| 比值 | DTMB 5415 (L=142 m) | 10 m USV | 差异 |
|---|---|---|---|
| λ/L | 1.0 | **14.7** | 15× |
| Hs/L | 0.023 | **0.33** | 14× |
| Hs/吃水 | 0.53 | **4.1** | 8× |
| Tp/T_heave | 1.9 | **5.4** | |

- 驱逐舰：跨在波上，纵摇接近共振，**基本线性**
- 10 m USV：贴着波面走（contouring），**经常整体出水再落下**

→ 非线性 FK 不是精细化，是必需。

---

## 2. 系统架构

```
Tier 0  几何与水动力数据库（离线，一次性）
        Wigley 参数化船型 → 网格 → Capytaine BEM
        → A(ω), B(ω), F_exc(ω, β)   [6×6，多浪向]
        → A∞ ,  K(t) via Ogilvie 关系
        → 状态空间实现 (vector fitting / Prony)
                    ↓
Tier 1  时域仿真器（Python，向量化）
        非线性 Cummins 6-DOF
        + 波场（JONSWAP + cos^2s 方向扩展）
        + 推进/舵 + 执行器模型（延迟、速率限、死区、通气）
        + 粘性阻尼修正 + 波浪增阻
        → Gymnasium 接口
                    ↓
Tier 2  控制器
        NMPC (acados)，内部 3–4 DOF 降阶模型
        预视接口（波场查询，时长与噪声可参数化）
                    ↓
Tier 3  学习
        RL / CMA-ES 调 MPC 权重，按海况调度
```

---

## 3. 代码结构

```
usv-rl-mpc/
├── hydro/
│   ├── geometry.py        # Wigley 船型、球、网格生成
│   ├── bem.py             # Capytaine 驱动 → 水动力数据库
│   ├── retardation.py     # Ogilvie 变换、状态空间实现
│   └── verify_sphere.py   # 对半解析解校验
├── sim/
│   ├── wavefield.py       # JONSWAP + 方向扩展（已有）
│   ├── vessel6dof.py      # 非线性 Cummins 6-DOF
│   ├── actuators.py       # 推进器/舵，延迟、限幅、通气
│   ├── corrections.py     # 粘性阻尼、波浪增阻
│   └── env.py             # Gymnasium 封装
├── control/
│   ├── mpc.py             # acados NMPC
│   └── preview.py         # 预视接口 + 传感器噪声模型
├── learn/
│   └── tune.py            # 权重调优（CMA-ES / PPO）
└── studies/               # 各种扫描实验
```

已有（Phase 0，将并入 `sim/`）：
`step0_preview_spec.py`（波场 + 预视几何）、`vessel.py`（降阶模型）、
`phase0_sweep.py`（MPPI 扫描）、`verify_model.py`、`explain_cummins.py`

---

## 4. 里程碑与验收门

每个里程碑都有**可证伪的验收标准**。过不了不往下走。

### M1 — BEM 管道 ✅ 已通过
- [x] Wigley 参数化网格生成，**二阶收敛**到解析值
      （40×14: V 误差 −0.14%，A_wp −0.03%；80×28: −0.04% / −0.01%）
- [x] 球体验证：**M1 BEM 门通过**
      - A′(ka→0) = 0.8479 vs Hulme 0.831（+2.0%）
      - A′(ka→∞) = 0.4995 vs 解析球极限 0.5（**−0.1%**）
      - 网格收敛 max ΔA′ = 0.0023；B′ 两端 → 0
- [x] 盖板消除不规则频率（无盖板 ω=4.43 有伪影，有盖板无）
- [x] 正式数据库 `hydro_wigley_10m.npz`：2400 面片、150 频率（0.1–15 rad/s）、
      13 浪向、6×6。**互易性 |A−Aᵀ|/max|A| = 6.9e−04，|B−Bᵀ| = 4.1e−04**

### M2 — 记忆核与状态空间
- [x] Ogilvie 变换 + 渐变窗（抑制截断引起的 Gibbs 振铃）
- [x] A∞ 提取（按可解析频带取平均，spread 作为截断诊断）
- [x] ERA 实现 + **慢模态剪枝**（按物理排除比记忆慢得多的伪极点）
- [x] **合成单元测试通过**（已知真值）：极点恢复 0.177%，
      A∞ +0.06%，K 往返 0.47%，B 0.027%，A 0.255%
- [x] **M2 门在真实船型上通过：28/28**

| dof | 阶 | A∞ | K 往返 | 波频带 B | 波频带 A | **记忆** |
|---|---|---|---|---|---|---|
| Surge | 10 | 174.6 | 0.13% | 0.18% | 0.36% | **2.8 s** |
| Heave | 10 | 11388 | 0.10% | 0.19% | 0.55% | **2.2 s** |
| Roll | 10 | 1484 | 0.07% | 0.05% | 0.66% | **2.5 s** |
| Pitch | 10 | 35184 | 0.08% | 0.12% | 1.04% | **2.9 s** |

**→ 这个船型的水动力记忆 = 2.2–2.9 s**（Phase 0 玩具模型的预视拐点是 ~4 s，同量级）

**M1/M2 踩到的六个坑（都已修，值得记住）：**
1. 网格生成 `base = len(verts)` 数的是数组不是顶点 → 舷侧索引错位
2. 验证脚本用线性外推穿过曲线峰 → 误报 BEM 失败（BEM 是对的）
3. Hankel 窗按采样数定 → dt 变细时只覆盖 K 的前 0.94 s
4. **配方：ERA 要多要几阶再按物理剪枝**，直接要目标阶数反而差 8 倍
5. netCDF 存不了 categorical dtype，且无 netCDF4 后端 → 改用 npz
6. **Ogilvie 符号错**：`Im[H] = −∫K sin dt`，所以 `A = A∞ + Im[H]/ω`。
   合成测试**用了同一个错误约定构造真值**，两边抵消、往返闭合，抓不到。
   只有带正确物理约定的真实 BEM 数据才暴露出来
   → 教训：自洽的往返测试无法发现约定错误，必须有外部真值

**指标也要看对地方**：Surge/Roll 的 B(ω) 到 ω=15 还剩峰值的 8%/4.6%，
渐变窗正在处理这个截断，所以窗区内的"误差"测的是窗不是模型。
误差应在窗区之外评估，真正要看的是波频带内的精度。

### M3 — 线性时域仿真器 ✅ 已通过（6/6）
- [x] 6-DOF 线性 Cummins，RK4，**108 个状态**（12 刚体 + 96 辐射记忆）
      辐射记忆只拟合 10 个非零 (i,j) 对——左右对称使纵向组与横向组不耦合
- [x] **门：时域 RAO 对频域解析解**
      迎浪，ω = 0.4–2.5 rad/s：**垂荡最差 0.28%，纵摇 0.10%，相位 0.2°**
- [x] **门：长波极限** 垂荡 RAO → **1.0000**（λ/L = 154 时随浪面起伏）
- [x] **门：自由衰减** T = 1.53 s（ω_n = 4.12 rad/s），ζ = 0.234，稳定
- [x] 时间约定**实测判定**而非假设：`e^{+iωt}`（e^{-iωt} 给出 7.8° 相位误差）

**物理确认**：ω_n = 4.12 rad/s，而 SS5 谱峰 ω = 0.65 rad/s → ω/ω_n = 0.16，
深在 **contouring（贴浪）区**——正是之前从 λ/L ≈ 15 推断的结论，现在由模型独立确认。

### M4 — 非线性与修正 ✅ 已通过（7/7）
- [x] **弱非线性"增量"FK**：`τ = τ_BEM线性 + (τ_条带非线性 − τ_条带线性化)`
      小幅时括号项→0，精确退化为 BEM。线性极限误差 **0.21%**
      （直接用条带 FK 替代是 10.4%，因为条带理论在 λ/L→1 时误差增长）
- [x] 条带几何三方一致：解析 / BEM / 数值刚度，C33 误差 0.002%，C55 0.010%
- [x] 波浪增阻（Gerritsma-Beukelman 辐射能量法，按 beam² 分布剖面阻尼）
- [x] 粘性阻尼（占位值，待自由衰减试验）
- [x] 执行器：滞后、传输延迟、速率限、死区、**推进器通气**
- [x] 前进速度：船在自己的移动位置查询波场，遭遇频率是结果不是参数
- [x] 不规则波：JONSWAP + 方向扩展 + 逐分量绕射
- [x] 门：静水平衡 / 线性极限 / 稳态航速 / 增阻生效 / 通气单调 / SS5 有界

### M5 — Gymnasium 接口 + MPC ✅
- [x] MPPI，节点参数化（7 节点而非 24 步，采样才探索得动）
- [x] **预视是一等参数**（时长 + 噪声），扫描无需改控制器
- [x] MPC 内部用**从已验证 plant 辨识出来的降阶模型**，不是拍脑袋
- [x] `USVEnv`：动作 = **MPC 权重**，不是舵。这是能在真船上收尾的关键

### M6 — 权重调优 ✅ 已通过（2/2）
CMA-ES，14 代 × 8，7 个有界权重。**训练分 0.6956 → 0.5558（+20.1%）**；
换 4 个未见过的波浪实现（seed 7,8）复验：**0.9238 → 0.6959（+24.7%）**，
提升在**没见过的海面上更大**，不是过拟合。

调出来的权重本身就是结论：`w_speed` 被压到下界 **0.1**，`w_acc` 抬到 **8.8**，
`w_dthrust` 归 **0** ——优化器自己发现，在 SS5 里**保航速不值得**，
而且推力变化率不该罚（罚它就等于禁止船去躲浪）。


- [x] 紧凑 CMA-ES（7 维不值得引依赖，写出来也便于复现）
- [x] 目标是**操作方的**目标，不是 MPC 自己的代价——否则只是教它同意自己
- [x] 留出未见海况实现做泛化检验

### 自由面重建 ✅ 已加（`hydro/freesurface.py`）
- [x] 从同一套 BEM 解出绕射势和辐射势，在自由面场点上求值
      `η = η_I + η_D + Σⱼ ξⱼ(ω)·η_R(ω,j)`，按频率合成一张复数传递函数表
- [x] 16 频率 × 61×41 场点，`freesurface_wigley.npz`

**扰动大小实测**（相对波幅）：

| ω | λ/L | max\|η_扰动\| |
|---|---|---|
| 0.65（SS5 谱峰） | 17 | **0.2%** |
| 1.40 | 3.1 | 13% |
| 2.20 | 1.3 | 53% |

**在真实 SS5 海况中：扰动峰值 0.042 m，而波浪 ±2.2 m ≈ 2%。**

→ **预视链路"只用入射波"的假设，对这条船这个海况是成立的。**
之前标记为局限的那一项，量化后是小的。短波尾部才显著，而那里没有能量。

### 定常 Kelvin 波系 ✅ 已补（`hydro/kelvin.py`）

上面那条"仍保留的局限"已经补上。**船一直是有航速的**（运行中 3.5–5.5 m/s，
遭遇频率是它在自己移动的位置上查波场自然产生的）；缺的是**水动力系数
$A(\omega),B(\omega)$ 按零航速解**，定常波系正是从这个缝里漏掉的。

量级对比（$U=4.6$ m/s，$Fn=0.46$）：滞止水头 $u^2/2g=1.08$ m，横波波长 13.6 m
——真实艏波是那 0.042 m 非定常扰动的 **10–25 倍**。这解释了"为什么可视化里
看不到船造的波"。

- [x] Michell 薄船理论，$x$/$z$ 积分均为闭式
- [x] 幅值按能量标定到本模型自己的阻力（`RW_FRACTION = 0.45`）
- [x] 6/6 验收通过：横波波长与理论**完全吻合**，半角 20.8–22.3° vs 19.47°
- [x] 烘焙成 384×256 贴图接入 WebGL 查看器，按 $(u/U_0)^2$ 相似性拉伸

| U (m/s) | Fn | 横波波长 理论 / 实测 | 半角 | 峰值 |
|---|---|---|---|---|
| 3.5 | 0.35 | 7.8 / **7.8** | 22.3° | 0.43 m |
| 4.6 | 0.46 | 13.6 / **13.6** | 21.5° | 0.39 m |
| 5.5 | 0.56 | 19.4 / **19.4** | 20.8° | 0.46 m |
| 6.5 | 0.66 | 27.1 / **27.1** | 21.0° | 0.61 m |

**不回灌到受力**：兴波阻力已经在拟合的二次阻力里，加两遍是重复计。

**对预视方案无影响**：深水中 Kelvin 波系整个落在 19°28′ 楔形**之后**，前视传感器
根本看不到它——这一点反而是加了它之后才被真正验证的。

### M7 — 扫描 ⚠️ 结论已撤回，见 DEFECTS.md A22 / A23

原报的"预视把砰击降低 31–47%、拐点 ≥6 s"**不成立**：它建立在每档 2–7 次事件上，
比值的噪声是 59%。12 个种子 + 误差棒重跑后，0–12 s 六档没有一项显著；补测的
90 s 波群档同样无效，且显著项都是**变差**。

**当前结论**：对这条船、这个海况、仅有推力通道时，**预视没有可测的价值**。
原因不在传感：τ_u = 6.30 s vs T_e = 7.48 s，执行机构比扰动慢 3.4 倍。

**这不是终点，是把问题问对了**：预视要有价值，需要一个带宽跟得上遭遇频率的
执行机构（压浪板/截流板/主动水翼），或者工况落在共振区（Tp ≈ 2.9–3.7 s）。
两者都是可以下一步验证的具体命题。

### M7（旧）— 扫描 ✅ 已实现
- [x] 预视扫描 + 恒推力 Pareto 基线（等航速下比较）
- [x] 限定长峰迎浪 + 锁舵（见 DEFECTS.md C1）

---

## 5. 已知缺口与处理方式

| 缺口 | 处理 | 何时 |
|---|---|---|
| 横摇粘性阻尼 | 自由衰减试验 > CFD > Ikeda | Phase 2 |
| 波浪增阻 | Gerritsma-Beukelman / STAWAVE-2 | M4 |
| 推进器通气 | 经验推力损失曲线（浸深函数） | M4 |
| 前进速度效应 | BEM 零航速；必要时切片理论 | 记录为限制 |
| 波面非高斯性 | 二阶波浪模型 | 后续 |
| 砰击载荷 | 只做发生判据（Ochi），不做载荷 | 记录为限制 |

**域随机化只能补参数误差，补不了缺失的物理。** 上表前三项必须显式建模。

---

## 6. Sim-to-real 计划

| 手段 | 成本 | 优先级 |
|---|---|---|
| **自由衰减试验**（垂荡/横摇/纵摇） | 码头一天 | **最高** |
| 系统辨识（Z 形、回转、螺旋） | 几天 | 高 |
| 执行器台架测试 | 几天 | **高**（文献第一失败源） |
| 域随机化 | 算力 | 中 |
| 分级验证：水池→湖→近海→SS3→SS4→SS5 | 数月 | 必需 |
| 实船权重调优 | 海试 | **绝对性能的真正来源** |

---

## 7. 待定决策

| 问题 | 影响 | 状态 |
|---|---|---|
| 排水型 / 半滑行 / 滑行？ | Fn>0.9 则势流失效，整条路线改 | **未定** |
| 目标航速范围 | 遭遇频率、孔径需求、砰击机理 | **未定** |
| 夜间作业？ | 立体视觉能否单独担纲 | 未定 |
| 主尺度（长、排水量） | 参数扫描的中心点 | 未定 → 先扫范围 |

**当前策略：船型不定不阻塞。** 用 Wigley 参数化 + 敏感性扫描，
把"没定船型"从障碍变成一个可回答的问题。定下来后换 STL，管道不变。

---

## 8. M7 结果：预视价值（已在验证过的模型上重算）

等航速下对比恒推力 Pareto 基线，SS5 长峰迎浪：

| 预视 | 99 分位加速度 | 砰击率 |
|---|---|---|
| 0 s | +1.3% | −2.9% |
| 2 s | +4.0% | +5.5% |
| 4 s | +3.7% | +9.0% |
| **6 s** | +2.5% | **−31.6%** |
| **8 s** | +2.4% | **−38.5%** |
| **12 s** | +2.5% | **−46.7%** |

**与 Phase 0 玩具模型的对比：**

| | Phase 0 玩具 | 验证过的模型 |
|---|---|---|
| 定性结论 | 预视买尾部风险不买平均运动 | ✅ **一致** |
| 拐点 | ~4 s | **≥6 s，12 s 仍在改善** |
| 砰击降幅 | −20~25% | **−32% ~ −47%** |

**拐点翻倍的原因**：辨识出的纵荡时间常数 τ_u = **5.9 s**，而玩具模型拍的是 3.0 s。
船对推力的响应慢了一倍，就需要大约一倍的提前量。这正是做验证的意义。

**加速度上预视始终不占优（+2~4%）**——控制器在这个指标上还不如恒推力，说明
手设权重没调好。这是 M6 要解决的，不是预视本身的结论。

**诊断已排除"控制器用不了预视"**：8 s 预视下推力指令波动是无预视的 **6.4 倍**，
信息确实到达了执行器。

### 对传感器方案的影响 ⚠️

需要的预视从 4 s 变成 **≥6 s，最好 8–12 s**。回到 Step 0 的孔径表：

| 需要 | 孔径 | 12 m 桅杆 + 立体视觉（可达 80–100 m） |
|---|---|---|
| 4 s | [10, 60–80] m | ✅ 够 |
| **8 s** | ~[15, 120] m | ⚠️ **临界** |
| **12 s** | 更大 | ❌ **不够** |

**结论翻转：桅杆方案从"够用"变成"临界到不够"，无人机方案重新回到桌面。**
这个改变直接来自把玩具模型换成验证过的模型——也正是当初坚持先建管道的理由。
