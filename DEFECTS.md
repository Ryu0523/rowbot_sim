# 缺陷审查

逐模块审查 M1–M7 的结果。分三类：**已修复的真缺陷**、**故意保留的局限**、**必须标定的占位参数**。

---

## A. 已修复的真缺陷

### A1 — 网格生成顶点索引错位（M1）
`base = len(verts)` 数的是**数组个数**（0 然后 1），不是累计顶点数。左舷面片索引到了右舷的顶点上，1120 个面掉到 600 个，体积误差 −14%。
**征兆**：面片数不对。**修复**：`base = sum(v.shape[0] for v in verts)`。

### A2 — 验证脚本穿过曲线峰做线性外推（M1）
用 `ka[:6]` 外推到 ka→0，但那 6 个点跨到 ka≈0.53，**穿过了 A′(ka) 的峰**。给出 0.912 而真值 0.831，误报 BEM 失败。
**教训**：**被测对象和测量方法都会错**。失败时第一件事是分清是哪一个。修复后改用对数频率网格，并加了第二个我自己推导的解析极限（ka→∞ → 0.5）作为交叉验证。

### A3 — Hankel 窗按采样数而非时间定尺寸（M2）
`r = min(N//2, 300)` 固定 300 个采样点。当 dt 从 0.05 细化到 0.0016 时，窗口只覆盖 K(t) 的**前 0.94 秒**，而 K 实际衰减要 8 秒——实现算法在从几乎没有信息的片段外推。
**修复**：按记忆长度的倍数抽稀，窗口在**时间**上定尺寸。

### A4 — Ogilvie 符号错，且合成测试无法发现 ⚠️
`H(iω) = ∫K e^{−iωt}dt` ⇒ `Im[H] = −∫K sin dt`，所以 `A(ω) = A∞ **+** Im[H]/ω`。我写了减号。
**征兆**：`errA` 对四个完全不同的自由度都给出 ~200%，且数值几乎相同——**不同输入给出相同误差，是 bug 的指纹**。
**为什么合成测试漏掉**：我在构造真值时用了同一个错误约定，两边抵消、往返闭合。
> **自洽的往返测试无法发现约定错误。** 必须有携带正确约定的外部真值。

### A5 — 误差指标在测渐变窗而不是模型（M2）
Surge/Roll 的 B(ω) 到 ω=15 还剩峰值的 8%/4.6%，渐变窗正在平滑这个截断。在窗区内拿拟合值对比原始 BEM，测到的是窗。
**修复**：误差只在窗区外评估，并单列 SS4–5 波频带内的精度（那才是有意义的数）。9.3% → 0.3%。

### A6 — 假的"实测符号判定"（M4a）
测试声称从 C55 判定 `sign_pitch`，但两个符号给出**完全相同**的 C55——符号在浸深和力臂里各出现一次，平方掉了。
**修复**：改为从 Capytaine 的 dof 定义**推导**（绕 +y 转 ⇒ δz = −θx ⇒ sign = −1），并说明 C55 为何无法判定它。

### A7 — Smith 因子深度重复计算（M4）
`centroid_depth` 返回的已经是相对设计水线的深度（满载约 −0.375T），我又减了一个 T。
**征兆**：线性极限误差随频率单调增长（2.2% → 21.4%），响应被系统性低估。验算 e^{−kT} 与观测吻合。

### A8 — 条带 FK 直接替代 BEM FK（M4）
条带理论在 λ/L→1 时误差增长（λ/L=1.3 时 11.7%）。这是**已知固有局限**，不是 bug，但不该让它作用在全部力上。
**修复**：改用弱非线性"增量"格式 `τ = τ_BEM线性 + (τ_条带非线性 − τ_条带线性化)`。小幅时括号项→0，精确退化为 BEM。线性极限误差 10.4% → **0.21%**。

### A9 — 推进器延迟线被污染（M4）
`deriv` 里调用 `prop.step`，而 Heun 每步调用 `deriv` **两次**，每次消耗一格传输延迟。
**修复**：拆成纯函数 `effective()`（可在导数里调）和有状态的 `advance()`（每控制步只调一次）。

### A10 — 每个 Episode 重新拟合辐射记忆（M5）
`RadiationMemory` 每次构造要拟合 10 个系数对（约 2 s）。调参循环有几百个 episode，**同一条船**，参数完全不变。
**修复**：按 `db` 缓存。同理缓存激励力传递函数——它只依赖分量的 (ω, 浪向)，与随机相位无关，所以所有种子共用。

### A11 — 降阶模型的纵荡等效质量偏差 32%（M5）
`m_eff = k·u_max·τ`，正确的线性化是 `m_eff = 2·k·u_ss·τ`。因为 `u_max = 1.35 u_ss`，模型的纵荡响应快了约 30%。

### A12 — `bow_acceleration` 重复求导（M5）
Episode 每个子步先调 `bow_acceleration`（一次完整求导）再调 `step`（两次求导），把 plant 成本提高了 50%。
**修复**：`step` 里的 k1 已含 nu_dot，直接存为 `last_bow_acc`。顺带修正了误导性签名——它不该接受指令值，因为力取决于**执行器状态**，而状态滞后于指令。

### A13 — MPPI 热启动移位约 4 倍过头（M5）
`nominal` 存的是 7 个节点，我按数组索引移一格，实际对应 (24−1)/(7−1) ≈ 3.8 个时域步。等于每个控制步把计划往前甩近 4 步。
**修复**：展开到时域 → 移一步 → 重采样回节点。

### A14 — 航速包线不真实，权衡消失（M5）
纵荡阻力和最大推力是随手填的占位值，导致 `u_max = 2.46 m/s` 而目标航速 4 m/s——**速度项饱和，控制器只会全速前进，没有权衡可探索**，整个预视研究会失去意义。
**修复**：按 10 m / 9 t 半排水艇的实际量级重设（6 m/s 时约 10 kN），u_max = 7.2 m/s。

### A15 — 长峰波会除零（M7）
`n_dir=1` 时方向扩展函数的归一化积分退化为 0。显式处理。

### A16 — 代价函数里的死项
`w_track * (...)**0 * 0.0` 恒等于零；降阶模型当时也没有 y 坐标，横向偏差根本算不了。补上 y 状态和真实的横向偏差项。

---

### A17 — Kelvin 谱在大方位角处被数值积分毁掉（Kelvin）
$z$ 方向的 $\int(1-(z/T)^2)e^{kz}\mathrm{d}z$ 原来是数值积的。大方位角处
$k=k_0\sec^2\theta$ 到几十每米，$e^{kz}$ 在一个格子内变化超过 2 倍，谱不再衰减
而变成噪声。**改为闭式解**（$x$、$z$ 两个积分都有）。
**症状**：半角与波长同时错，看起来像理论错。

### A18 — 三次测错波长，都是尺子的问题（Kelvin）
1. **数零点**：中线上同时有发散波，会多出交点 ⇒ 系统性偏短 8–12%。
2. **改 FFT 仍偏**：分析窗只有 45 m，在 27 m 波长下不到 1.7 个周期，答案只能落在
   45.1 / 22.6 / 15.0 / 11.3 … 的**频格阶梯**上——正是实测返回的那串数。
3. **抛物线细化把结果炸成 −0.0**：峰值处二阶差分是**负**的，而我用
   `max(den, 1e-30)` 去"保护"它，符号被翻转，细化跑到无穷。
**修法**：专用长中线 + 补零 FFT + 按**幅值**保护的抛物线细化（并把偏移限制在半格）。
**结果**：7.8 / 13.6 / 19.4 / 27.1 m，与理论**完全吻合**。
**教训**：原始频格早就对到 0.1%，错的一直是细化。

### A19 — 两次把"网格画不出来"误判成"没收敛"（Kelvin）
- **网格混叠**：$\theta_{\max}$ 是常数 1.45 时，$U=3.5$ 处波长 0.12 m 而网格 0.23 m，
  短波折叠回格点。**线索**：误差在**最低航速**最大——真正的截断误差不会这样。
  改为按 Nyquist 定 $\theta_{\max}$。
- **方位角积分混叠**：相位对 $\theta$ 的导数 $\sim k_0R\sec\theta\tan\theta$ 在 80° 处
  达 1600 rad/rad，固定 401 个样点每步走 2 rad 以上。**线索**：加密网格误差反而**变大**
  （7.1→9.6%），因为加密会放开 $\theta_{\max}$，而那正是采样失败的地方。
  改为按相位速率定样点数，并分块累加（否则数组要几个 GB）。
- **单像素最大值当收敛指标**：楔形边缘是**焦散**，一两个格子宽，最大值差主要在报告
  这道脊落在格子的哪个位置——加密时读数 7.1/4.6/5.3/15.4% 毫无趋势，而同一批场的
  半角稳定在 0.1° 内。改用 RMS。

### A20 — 重新导出静默丢掉了扰动表（可视化）
`studies/export_viewer.py` 从来没有写过 `disturb` 字段——之前那份 JSON 里的是手工
补进去的。加了 Kelvin 后重新导出，查看器在 `DATA.disturb.omega` 上直接抛异常，
整页只剩一行报错。**修法**：把扰动表的导出写进导出脚本本身。

### A21 — 在暂时性死区里调用 `el()`（可视化）
新代码在第 ~235 行用了 `el("k-info")`，而 `const el = ...` 在第 472 行。`const` 会提升
但处于 TDZ ⇒ `ReferenceError`，整个查看器白屏。改为直接 `document.getElementById`。
**这一条是发布前查出来的，没有到用户手里。**

---

### A22 — M7 的头条结论建立在数不清的稀有事件上 ⚠️ 最贵的一次
砰击率 0.2–0.7/min，3 个种子 × 200 s = **10 分钟曝光 ⇒ 每档只有 2–7 次事件**。
Poisson σ = 38–71%，两个计数相比，比值的 σ 是 **59%**。而报出去的效应量是 30–47%
——**约 1 个 σ**。12 个种子重跑后，六个预视档位没有一项超过自身误差棒。

原来的 **−31.6% / −38.5% / −46.7%（拐点 ≥6 s）作废**，以及由它推出的
"需要无人机而不是桅杆"。

**修法不是加种子硬撑，是换掉计数**。见 `sim/seakeeping.py`：
Ochi 的率公式只要两个方差就能解析给出砰击率，

  P = exp[−T²/(2σ_r²) − v_cr²/(2σ_ṙ²)]，  N = (1/2π)(σ_ṙ/σ_r)·P

同一个物理定义，但 σ 收敛按 1/√采样数而不是 1/√事件数。实测两者均值相符 17%
以内（0.271 vs 0.325 /min），**离散度小 5 倍**。

12 个种子下各指标的相对标准误（这张表本身就是结论）：

| 指标 | SE/均值 |
|---|---|
| u_mean（航速） | 0.2% |
| acc_rms / rvm_rms | 0.4% |
| slam_impulse | 1.1% |
| acc_p99 | 1.2% |
| fatigue | 1.3% |
| rvv_rms | 1.6% |
| slam_p99 | 2.2% |
| wet_rate（甲板上浪，解析） | 2.3% |
| slam_rate_ochi（解析） | 5.8% |
| **slam_per_min（计数）** | **24.0%** |

**当初选中的指标，正好是手上最差的那一个，比最好的差 100 倍。**

### A23 — 预视本身在推力通道上无效，原因是执行机构不是传感器
两个数就够了：纵荡时间常数 **τ_u = 6.30 s**，遭遇周期 **T_e = 7.48 s**。要在一个
波浪周期内改变响应需要 τ ≪ T_e/4 = 1.87 s，唯一的执行机构**慢 3.4 倍**。
所以 2 s 与 12 s 预视结果完全相同——不是够了，是两个都用不上。
（MPC 视界本身也是 24×0.5 = 12 s，扫描右端点正好卡在自己的天花板上。）

波群尺度（50–100 s）τ_u 是够快的，于是补测了视界/预视 90 s 的一档：

| 方案 | u | acc_p99 Δ% | 显著 | slamImp Δ% | 显著 |
|---|---|---|---|---|---|
| 无预视 | 4.19 | +2.5 ±1.3 | no | +0.9 ±1.5 | no |
| 短（M7 的 8 s） | 4.58 | +2.8 ±1.3 | **YES** | +1.6 ±1.4 | no |
| 波群（90 s） | 3.87 | +2.9 ±1.7 | no | +6.9 ±2.9 | **YES** |

**等航速下每一档都比恒推力基线略差**，显著的两项都是**变差**。推力调制本身
（纵荡振荡耦合进纵摇）比预视信息带来的收益更贵。

→ 唯一有效的杠杆是**选一个更低的平均航速**，而那根本不需要预视。
→ 硬件问题从"桅杆还是无人机"变成"要不要压浪板"——**瓶颈在带宽，不在视距**。

保留的前提：本次用的是**手调权重**；M6 调出的权重是对着一个主项偏 9% 的代价
函数调的（见砰击载荷），两者都需要重做后再下最终结论。

---

### A24 — MPC 的内部模型是数值发散的 ⚠️
降阶模型用**前向欧拉**积分升沉和纵摇，而 dt = 0.5 s、ωn = 3.09 / 3.57 rad/s
⇒ ω·dt = 1.5–1.8。前向欧拉在振子上**无条件不稳定**，放大因子
√(1+(ωdt)²) ≈ 1.8–2.1 每步。

`studies/model_horizon.py` 给模型一切优势（精确初态、真实未来推力、**完美未来波场**）
后实测：误差/信号 12 秒内涨到 **1.8×10¹²**，相关性 1 秒后跌破 0.5。
**MPC 有 90% 的视界是在对着噪声做优化。**

它没炸，只是因为每 0.5 s 重规划、只执行第一步。

**修法**：升沉/纵摇是线性二阶系统，用闭式转移矩阵 `expm(A·dt)`（缓存），无条件稳定；
首摇也改成精确一阶更新。

修完误差不发散了，但幅值系统性偏大 **2.1 倍**——因为波浪激励在步内被冻结（零阶保持），
模型看到阶梯，而 `z̈ = ω²(η̄−z)` 里 ω²=9.5 把阶梯边缘放大。代价函数本来就在步内积分，
所以加速度改取**步内平均**。

| 提前 | 相关性 修前→修后 | 幅值比 修前→修后 |
|---|---|---|
| 1.5 s | 0.53 → **0.88** | 2.10 → **0.84** |
| 3.5 s | 0.52 → **0.90** | 2.10 → **0.78** |
| 7.5 s | 0.62 → **0.81** | 2.11 → **0.81** |
| 11.5 s | 0.58 → **0.64** | 2.13 → **0.82** |

**这一条把 A23 的否定结论一度整个悬置**——"预视没用"必须先排除"控制器用不了预视"。
排除之后结论不变，但强度提高了。

诊断脚本自己也有一个错：真值取了 `hist[i+h+1]`，错开一个控制步，把幅值问题显示成
**负相关**，看起来像符号错。又是尺子。

### A25 — 等航速比较被静默钳位
`np.interp` 超出范围返回端点值而不报错。group 档跑在 3.62 m/s，而基线最低 3.86 m/s
⇒ 它被拿去和一个**快 0.24 m/s** 的基线比。慢下来改善一切，所以这凭空造出了改善：
修 A24 后那一轮 9 项里 7 项"变好"，**全部是这个假象**。

基线延到 3 kN（u=3.07）后，同样的档位变成**显著更差**（砰击冲量 +14.0%、
砰击力 p99 +22.3%，均超 2σ）。`interp` 现在超范围直接抛异常。

和 sea_state_value 最优点落在区间边界是同一类错误：**边界上的答案是区间的答案，
不是物理的答案。**

---

### A26 — 舵的最大角超过它自己的失速角
最大舵角 35° 而失速角 28°，所以**最后 7 度行程让船转得更差**：25° 半径 2.27 L，
35° 反而 2.70 L。没人会这么设计。

**修法是抬高失速角而不是砍最大角**：35° 是几乎所有船的标准最大值；而舵位于**螺旋
桨尾流**中，加速的滑流给边界层补能，失速比同样翼型在自由来流中晚得多。实际设计
就是让失速点落在限位之外。改成 38° 后操纵门 **6/6 通过**（35° 舵半径 1.63 L，单调）。

仍是占位参数，但现在**自洽**，并且被 `sim/test_manoeuvre.py` 框定。

### A27 — 降阶模型没有波浪转艏力矩，转向预测 1–2.5 秒就废
加了横荡之后 MPC 终于能转舵（航向 RMS **476° → 0.9°**，横偏 0.3 m）。但
`studies/model_horizon.py` 扩展到转向通道后发现：

| 通道 | 舵死锁 | MPC 转舵 |
|---|---|---|
| 艏加速度 | 12.0 s | **12.0 s** |
| 航向 | 12.0 s | **2.0 s** |
| 横偏 | 12.0 s | **2.5 s** |
| 转艏率 | 1.0 s | **1.0 s** |

根因：降阶模型的转艏**只由舵驱动**（一阶 Nomoto），**没有任何波浪激励**。真船在
浪里转艏率主要被波浪力矩支配，模型看不见。

**这把两件事分开了**：
- **调节**只需第一步准（航向相关性 0.959、横偏 0.988）⇒ 闭环转舵**可用，已部署**
- **预判**需要全视界准 ⇒ "预视能否帮助航向控制"**暂时测不了**，会测成模型局限

下一步要补的是波浪引起的横荡力和转艏力矩。现有结构的自然扩展：站位采样加上
**横向偏移**（±B/2），像纵摇取纵向斜率那样取横向斜率。

---

### A28 — 预视采样点根本不跟着船走
`PreviewProvider.at` 把 **y 硬编码成 0**，`rollout_cost` 用 `xs = x + x_st` 完全
**不考虑航向**。也就是说控制器一直在 x 轴上、按船头朝 +x 采样海面，不管船实际在哪、
朝哪。

直着走 + 锁舵时无害——**而所有预视研究都是这个配置**，所以从没暴露。一开始转舵就全错。

已修：`at(x, y, t, t_ahead)` 取真实坐标，站位按预测航向旋转。闭环横偏 0.30 → 0.28 m。

### A29 — 波浪转艏/横荡的几何参数化行不通（已回退）
为解开 A27，给降阶模型加了重力分量形式的波浪水平激励：船在横向倾斜的水面上会
往下滑（横荡），艏艉倾斜不同则产生转艏力矩。量纲干净，系数从船体回归而非猜测。

**它什么也解释不了**（3×180 s 短峰浪回归）：

```
  横荡   相关性 +0.066   R² 0.004
  转艏   相关性 +0.003   R² 0.000
```

而且特征幅值和响应同量级（std 0.248 vs 0.215），**不是尺度问题，是时间上对不上**。

原因：波浪激励**不是瞬时局部几何**，而是 BEM 激励传递函数，带**随频率变化的相位
滞后**。作用在瞬时几何量上的实数系数**无法表示相位**。

`model_horizon` 的可用视界没有任何变化（航向仍 2.0 s、转艏率仍 1.0 s），
**预注册的停止条件生效 ⇒ 回退**。系数钉死为 0，`_fit_wave_horizontal` 保留以便复现。

**真正的修法**：让降阶模型携带波浪分量，按分量应用复数激励传递函数（和船体一样）。
代价估算 64 分量 × 24 步 × 192 轨迹 ≈ 295k 次复数求值，约 10 倍现有推演成本
（7.4 ms → 74 ms），仍在 500 ms 预算内。**但它会打破预视抽象**——控制器将直接
接触谱，包括预视窗口之外的部分，必须同样在窗口外置零，那是预视接口的重新设计。

---

### A30 — 刚体惯量矩阵错了 17–23%，而所有测试都通过 ⚠️ 最能说明问题的一次

`db.M` 来自 Capytaine 的 `compute_hydrostatics()["inertia_matrix"]`。对这条船它是错的：

| 自由度 | 存储值 | 正确值 | 误差 |
|---|---|---|---|
| 横摇 I44 | 2003 | 2588 | **−22.6%** |
| 纵摇 I55 | 38250 | 45912 | **−16.7%** |
| 艏摇 I66 | 39613 | 47787 | **−17.1%** |

**为什么之前一个测试都没抓到**：排水体积对（+0.07%）、垂荡刚度对（0.1%）、横摇刚度 C44 对（+0.19%）、`audit_physics` 27/27 全过。**没有一项检查惯量**。它在整个动力学方程的分母里。

**原因，值得单独记住**：体积矩可以用三种等价的面积分写出来

    ∫x²dV = ∮ n_x(x³/3)dS = ∮ n_y(y·x²)dS = ∮ n_z(z·x²)dS

Capytaine 把三种都算出来然后**取平均**。对球体这是好主意——三种都良态，平均能抵消离散噪声。对这条船不是：Wigley 船体近乎垂直壁，整个侧面 n_z≈0，n_z 那一项完全由龙骨附近一小条近水平面元承担，而那里型宽已经趋于零。实测同一套网格：

    ∫x²dV   n_x 形式 44.431   n_y 形式 44.440   n_z 形式 22.216
    ∫y²dV   n_x 形式  2.175   n_y 形式  2.175   n_z 形式  0.589

两种一致到 0.02%，第三种差**一倍**。平均之后，一个 100% 的错误被埋成 17%——大到影响结果，小到看起来像离散误差。

> **把本应完全相同的三个估计取平均，会毁掉你唯一能发现其中一个坏掉的证据。先比较，再平均。**

**修复**：`hydro/inertia.py`。只用两个良态形式，并且**互相比对**，不一致超过 2% 直接抛异常（不一致意味着网格有问题，那是另一个问题，不能被平滑掉）。

**验证用的是外部真值，不是本项目的任何东西**：
- 半浸实心半球：绕球心任意轴 I = (2/5)mR²（精确）
- 半浸长方体：I_xx = m(b²+c²)/12（精确）
- Wigley 船体：闭式解，推导写在 `wigley_inertia()` 里

最差误差 0.34%，全部是网格离散。

**影响**：`hydro/fix_inertia_db.py --apply` 已写回。固有周期：横摇 1.56 → 1.68 s，纵摇 1.86 → 1.96 s，垂荡不变。**ω_e/ω_pitch 从 0.249 变成 0.262——"刚度控制"的结论不变**。但 PPO/BC 的权重、`bc_demos.npz` 全部作废（它们是在错的质量矩阵下训练的），需要重训。

**同时暴露一个更大的问题**：uniform density 本身就是假设。真实小艇 k_roll = 0.33–0.40 B、k_pitch = 0.24–0.26 L，而这条裸船壳的几何值是 0.213 B / 0.225 L。修好几何只是把 bug 去掉，假设还在——所以 `hydro/inertia.py` 把 gyradius 做成显式参数，并用 `sensitivity()` 打印整个区间上固有周期的变化，让人没法在看不到代价的情况下用它。

---


## B. 故意保留的局限（已在代码中声明）

| 局限 | 影响 | 何时处理 |
|---|---|---|
| 水动力系数零航速 | 遭遇频率对 A(ω)、B(ω) 的真实影响缺失 | 需切片理论或带速格林函数 |
| 横摇回复力仍线性 | 条带积分只做纵向 | 加航向控制时 |
| 波面高斯 | 极端波峰被低估（真实 SS5 偏度 +0.1~0.3） | 需二阶波浪模型 |
| 绕射线性且零航速 | — | 同第一项 |
| **降阶模型无横荡** | MPC 无法预测波浪driven的横漂，只能用航向追 | **见 C1** |

### C1 — 一个真实耦合，不是缺陷
短峰波下 `t_preview=6 s` 时横向偏差 RMS 达 21.8 m。原因是**舵力 ∝ u²**：为了改善运动而减速，会同时损失航向权限。这是真实物理，项目应当记住。

预视研究因此**限定在长峰迎浪**——航速调度本来就是迎浪问题，横漂是混淆项。短峰波的航向控制需要给降阶模型加横荡状态，属于 Phase 2 的工作。

---

## C. 必须标定的占位参数

这些数值目前是**有正确形状但未标定**的占位值，绝不能当结论引用：

| 参数 | 现值 | 需要什么 |
|---|---|---|
| 推进器通气推力损失曲线 | 平滑单调，1.5 倍桨半径以上全湿 | 螺旋桨试验 |
| 舵升力斜率与失速角 | 3.5 /rad，28° | CFD 或水池 |
| 推力滞后 / 速率限 / 死区 / 延迟 | 1.5 s / 8 kN/s / 50 N / 0.15 s | 实机台架 |
| 粘性阻尼（6 个自由度） | [280, 4000, 3000, 2500, 9000, 6000] | **自由衰减试验**（最高优先级） |
| 纵荡阻力 | 280 N/(m/s)² | 阻力试验或 Holtrop |

**横摇阻尼尤其危险**：势流给出的横摇阻尼接近零，占位值决定了横摇共振的全部行为。文献里纯势流对横摇的误差是 59–80%。

---

## D. 相比真实物理漏掉的项（`studies/missing_terms.py`）

A 节查的是**算错**，这一节查的是**没写**。

### D0 — 这一节的第一版方法就是错的 ⚠️

第一版按"占已有合力的比例"给缺失项排序，所有行并排呈现。问题是：**每一个量级都是模型自己的轨迹算出来的，除以模型自己的合力**。说某项"占横荡 24%"，用的横荡速度是模型在**没有这一项**的情况下产生的，分母同时还缺风、缺漂移力、缺舵来流角。测量工具和被测对象是同一个东西。

更糟的是我写了"2.32 L 在 10 m 半排水艇的合理区间中间"——那个区间是我编的。**用一个编出来的参考去验证一个改动，比没有参考更糟，因为它看起来像证据。**

A30 就是这种做法的实际代价。

所以这一节改成**按证据强度分层**，不按大小排序：

| 等级 | 依据 | 可能错在哪 |
|---|---|---|
| **A** | 代码事实：真实物理里有、源码里没有。由脚本**读源码**验证，不是断言 | 不会错 |
| **B** | 量级来自**模型之外**：船体几何 + 公开经验关系 | 只有公开关系错了才错 |
| **C** | 量级来自模型自己的运动 | 循环论证。附占位参数扫描区间，区间宽就说明这个排序不是信息 |

### D1 — Tier A：代码事实（脚本自动核对，不会过期）

每一条都是对源码正文的一个判定式。如果有人把这一项补上了，对应行会自己失效。

- ~~刚体科氏力：6 个分量里写了 2 个~~ ⚠️ **已撤回（E11）**：另外 4 个在本模型的坐标系里本来就不该有
- 附加质量科氏力：完全没有（该补的只是艏摇旋转产生的部分，E11）
- 风：`sim/` 里 grep 不到任何一个 wind
- 舵来流角：`force(angle, u)` 拿不到 v 和 r，物理上不可能知道来流角
- 舵通气：`submergence_factor` 存在但没有任何调用点传过它
- 螺旋桨：没有进速比曲线
- 回复力矩阵恒定：参数横摇不是"小"，是**无法表示**
- 绿水：只算指标，从不产生力
- 二阶波浪力：激励对波幅严格线性，漂移力按构造不存在
- 航行下沉纵倾：任何 Froude 数下都浮在静水线

### D2 — Tier B：外部锚定的量级

| 项 | 量级 | 来源 / 后果 |
|---|---|---|
| 风的横向力（横风） | 397 N | Pierson-Moskowitz 反推 U=11.4 m/s，侧面积 5.5 m²，C_Y 0.9 |
| 风的艏摇力矩 | 318 N·m | 0.08 L 力臂，需要常备舵角去配平 |
| 风的迎面阻力 | 77 N | 7 kN 推力的 1.1%——**这一条不需要模型就能判掉** |
| 螺旋桨 K_T(J) 纵荡阻尼 | 1307 N/(m/s) | Wageningen B 系列斜率；模型现有 2520，即 **+52%，且无滞后** |
| → 对纵荡时间常数 | τ_u 6.30 → **4.15 s** | "推力比 T_e/4 慢 3.4 倍"变成慢 **2.2 倍**——结论不变，余量变小 |
| 二阶漂移力（谱峰 147 m） | 1 N | (ka)³ = 0.0002，船对谱峰几乎透明 |
| 二阶漂移力（短波尾 25 m） | 257 N | (ka)³ = 0.031 |
| 质量分布：均质裸船壳 | k_roll 0.213B / k_pitch 0.225L | 公开小艇值 0.33–0.40B / 0.24–0.26L，见 A30 |

### D3 — Tier C：模型自产的量级（附占位参数扫描区间）

扫描：粘性阻尼 ×0.5 与 ×2，Yv'/Nr' ×0.5 与 ×2。

| 项 | 自由度 | 基准 | 扫描区间 | |
|---|---|---|---|---|
| 附加质量科氏 +A₃₃wq | 纵荡 | 42% | 33–48% | |
| 横摇回复力随波变化 | 横摇 | 42% | 41–45% | |
| 刚体科氏 −mwq | 纵荡 | 33% | 27–38% | |
| 刚体科氏 +mwp | 横荡 | 30% | 21–53% | **区间太宽，不可用** |
| 刚体科氏 m(vp−uq) | 垂荡 | 23% | 19–26% | |
| 绿水纵摇力矩 | 纵摇 | 4% | 1–5% | **不可用** |
| 绿水垂荡力 | 垂荡 | 1% | 0–1% | **不可用** |
| 附加质量科氏 −A₁₁uq / −A₁₁ur | 垂荡/横荡 | 0% | 0% | |

前三项在整个扫描区间内都在 27% 以上，这个结论是稳的。横荡那一项不是。

⚠️ **见 E11**：这张表里的科氏项是**体坐标系**的项。本模型的坐标系只随艏向旋转，它们在这里本来就不存在——量级是真的，“缺失”的判断是错的。

### D4 — 舵来流角：唯一动结论的一项，但**门测不出来** ⚠️

`Rudder.force(angle, u)` 收几何舵角。真实 α_eff = δ − γ·atan((v + r·x_rud)/u)，γ≈0.45。

| 舵角 | 现模型 R/L | 补来流角 | 漂角 |
|---|---|---|---|
| 10° | 5.62 | 8.56 | 6.3° → 4.4° |
| 25° | 2.27 | 3.30 | 12.6° → 9.5° |
| 35° | **1.63** | **2.32** | 15.8° → 12.4° |

**两个版本都通过 `sim/test_manoeuvre.py` 的全部检查**，包括借来的 IMO 上界。所以：

> 这个门**无法在两者之间做选择**。一个能接受被测量本身变化 42% 的门，证明模型不荒谬，不证明模型正确。

没有实船试验，我说不出哪个更接近真实。**不需要试验就能说的**：

1. 这一项在真实船上存在，**符号已知**
2. 它**永远减小**舵力，不会增大
3. ~~所以本模型的航向权限是**高估**，所有基于航向的改善都是**上界**，不是估计~~ **已被 E2 推翻**：这是只看来流角时的结论。Munk 力矩随附加质量科氏项进来后方向反转，两者量级相当、方向相反，现在只能说"不确定"
4. Yv'/Nr' 是在**没有这一项**的情况下调出来通过这个门的，已经吸收了它的一部分。**单独补来流角会重复计算**——两者必须对着一次实船回转试验联合重标

### D5 — 判定为可以不修的项

- **参数横摇**：水线面惯性矩确实波动 15.4%，接近 Mathieu 阈值 2ζ=20%。但共振条件是 ω_e/(2ω_roll)=1，而实际值在**整个质量分布区间**上都只有 0.10–0.18（`hydro/inertia.py sensitivity()`）。差 5–10 倍，不是边缘情况。
- **二阶漂移力（谱峰）**：1 N。λ/L=14.7。
- **绿水**：Tier C 区间显示它连自己的量级都定不下来，但上界只有垂荡的 1%。
- **迎面风阻**：推力的 1.1%，Tier B 直接判掉，不需要模型。

### D6 — 修的顺序

1. **舵来流角 + Yv'/Nr' 联合重标**——但这条**必须等实船回转试验**，现在改只是把一个未标定的模型换成另一个
2. 刚体与附加质量科氏项补全（纯代数，无新参数，纵荡 33%+42%，扫描区间稳） ⚠️ 只有艏摇旋转产生的那部分该补，见 E11
3. K_T(J)（τ_u 6.30 → 4.15 s）
4. 风（三个自由度）
5. 舵通气（复用螺旋桨曲线）

### D7 — 这一节**不能**证明的事

**本项目从未与任何物理实验比对过。** 现有的外部锚定只有两类：精确数学（解析静水力、Hulme 的球、`hydro/inertia.py` 的闭式解）和公开经验关系。这些能抓编码错误（A30 就是这么抓到的），**不能**说明模型是否描述了真实的 10 m USV。

能说明的是：

| 需要的数据 | 能标定什么 | 现状 |
|---|---|---|
| **Wigley 船型耐波性试验**（ITTC 联合试验、Journée 的 Delft 系列，多个 Froude 数下的升沉/纵摇 RAO） | 整个水动力链条 | **现成的公开数据，不需要任何新设备，还没做——这是当前性价比最高的一件事** |
| 自由衰减试验 | 6 个粘性阻尼占位值，尤其横摇（势流几乎不给横摇阻尼，占位值决定全部共振行为） | 需要实物或水池 |
| 回转试验 | Yv'、Nr'、舵来流角（三者不可分离，见 D4） | 需要实船 |

---

## E. 补全物理项与船型通用化

### E1 — 补上的六项，以及每一项对 35° 回转半径的实测影响

一次只加一项，从能复现旧结果 1.63 L 的基线开始（见 E5 为什么这一点必须强调）：

| 步骤 | 35° 回转半径 | 说明 |
|---|---|---|
| 旧模型 | 1.63 L | |
| + 舵来流角 | 2.32 L | 稳态回转中舵损失约一半攻角 |
| + 合速度动压 | 1.98 L | 漂移既减小攻角，也增大动压 |
| + 附加质量科氏（艏摇坐标系） | 0.66 L | **Munk 力矩**，失稳性，本表最大的单项，漂角 24.9° |
| + K_T(J) | 0.66 L | |
| + 舵升力线斜率改为计算值 | 0.68 L | Whicker-Fehlner 3.33 /rad，原硬编码 3.5（E6） |
| + Yv'/Nr' 重新辨识 | **1.79 L** | Nr' 0.0030 → 0.0060，仍在公开范围之内（E3、E11） |

> 这张表是 E11 之后按**正确坐标系重测**的。第一版里有一行"+ 完整刚体科氏 1.98 L"，终值是 1.86 L——那是在错误坐标系下测的，已替换。

新文件：`sim/forces.py`（科氏矩阵、风）、`hydro/symmetry.py`、`hydro/hull.py`、`studies/manoeuvring_identify.py`、`studies/external_methods.py`。

- **科氏**：刚体和附加质量两部分由同一个**结构上斜对称**的构造生成，不手写展开（手写展开正是符号错误的来源）。两个检验：200 个随机质量矩阵上 νᵀC(ν)ν = 0（最差 1e-14）；刚体部分退化为 Euler 方程 m(ω×v)、ω×(Iω)（4.9e-13）。 ⚠️ **这两个检验只验证代数，不验证坐标系**——按体坐标系全部补上是错的，见 E11。现在只保留艏摇旋转产生的部分。
- **风**：默认取生成该海况的风（Pierson-Moskowitz 反推 11.4 m/s），用**视风**。迎风 150 N（推力的 2.1%），横风 427 N。系数 cx、cy 和艏摇力臂是占位值。
- **K_T(J)**：T = ρn²D⁴K_T(J)，K_T 取线性。指令仍是牛顿、在设计航速下含义不变，新增的只是**航速依赖**：dT/du = −1103 N/(m/s)，是现有纵荡阻尼的 +44%，而且**没有滞后**。
- **舵来流角**：α_eff = δ − γ·atan((v + r·x_rud)/u)，γ = 0.45；动压用合速度。
- **舵通气**：两个用深度表达的阈值，构造函数里断言静水因子恰为 1.0。

### E2 — 这一轮推翻了 D4 的一个结论 ⚠️

D4 写过"本模型的航向权限是高估，所有航向改善都是上界"。那是只看舵来流角时的结论（1.63 → 2.32 L）。**Munk 力矩随附加质量科氏项进来之后，方向反过来了**（→ 1.26 L；在正确坐标系下是 0.66 L，见 E11）。Munk 力矩比来流角更强。

现在能说的只有：航向权限**不确定**，可行域 1.09–2.19 L（2.0 倍，E11 之后）。这是更弱、也更准确的说法。

### E3 — Yv'/Nr' 联合重标：反过来做

旧做法：调到回转圆"看起来合理"，再拿回转圆当"模型操纵性合理"的证据——循环论证，而且这两个值同时吸收了当时缺失的舵来流角和 Munk 力矩。

新做法（`studies/manoeuvring_identify.py`）：从**公开的无量纲导数范围**取值（|Y_v'| 0.010–0.040，|N_r'| 0.002–0.008），与不需要试验就能跑的检查求交集，**回转圆作为输出**，是一个可以被实船试验证伪的预言。

- 35 组里 13 组可行（E11 之后；第一版是 14 组）。借来的 IMO 上界单独去掉了 6 组本来可接受的——它是写给大船的标准，所以单独标出
- 可行域中心 Yv' = 0.025、Nr' = **0.0060** → 35° 半径 1.79 L（第一版是 Nr' = 0.0040 → 1.86 L，那是对着一艘在静水中摇晃的船辨识出来的，见 E11）
- **Y_v' 几乎不影响回转圆**（网格每一行几乎相同）：回转试验根本标不出 Y_v'，需要 PMM 或 Z 形试验
- 本船 L/B = 4.0、C_B = 0.44；公开范围来自 L/B 5–8、C_B 0.5–0.85 的商船。这是外推，已写明
- Munk 力矩用的附加质量取 A_∞（N_v' = 0.0069）还是 A(ω→0)（0.0136）差一倍，两端都在公开范围附近。取 A_∞，因为它与 Cummins 公式一致

### E4 — 面板网格的不对称被 u² 放大 ⚠️

补全科氏项后，两项从项目开始就一直通过的对称性检查失败了：迎浪长峰波中横漂 2.86 m、艏向 0.6°，而正确答案是**精确的 0**。（2.86 m 是在 E11 之前的体坐标系版本下测的；机理——A_∞ 交叉项乘 u²——在艏摇坐标系的附加质量科氏里同样存在，所以这个修复仍然必要。）

排查：零掉 BEM 的横荡、横摇、艏摇激励，**漂移一位小数都没变**——源头不是激励。逐项关闭新加的项，定位到附加质量科氏：A_∞ 里本应为零的交叉项（垂荡-艏摇 12.8、纵摇-横荡 −11.3、纵荡-横荡 −0.54）被 u² 乘上，直线航行时注入约 9 N·m 的艏摇力矩。

> **在它所在的矩阵里小到可以忽略的数，在用到它的那一项里不一定小。要看的是它的贡献，而贡献取决于乘在它上面的是什么。**

修复：`hydro/symmetry.py`。对称船体的垂向组（纵荡、垂荡、纵摇）与横向组（横荡、横摇、艏摇）之间的耦合项是**几何上的精确零**，强制为零并报告去掉了多少。`symmetric=False` 留给真正不对称的船。

这个缺陷**在本轮之前就存在**（M 和 C 里都有），只是旧的两项科氏没有把它乘上 u²。

### E5 — 分解的第一行没有复现已知答案

舵通气曲线的第一版把"叶顶以上水深"这个参数的含义搞错了，**静水里就把舵力砍到 51%**。被发现是因为逐项分解的**基线行**给出 3.17 L，而已知答案是 1.63 L。

> **任何分解都必须从复现已知答案开始。第一行对不上，后面每一行都不可信。**

修复：两个用深度表达的阈值，构造函数里断言静水因子恰为 1.0。

### E6 — 结论先写，数字后算

`studies/external_methods.py` 的第一版在计算舵升力线斜率**之前**就写好了"占位值经受住了检验"。算出来：硬编码的 3.5 /rad 比 Whicker-Fehlner 最有利的情况（根部完整镜像，3.33）**还高 5%**。输出同时打印了 "OUTSIDE" 和 "survives"。

修复：结论改为依赖计算结果；舵升力线斜率改为**由叶片几何计算**，导入的船自动得到与自己的舵相符的值。

3.5 很可能在替一个缺失项顶班——**螺旋桨尾流对舵的加速**，它提高的是动压而不是斜率。同一个尾流是 38° 失速角的理由，而模型引用了尾流却没有建模它。已列入 Tier A 缺失项。

### E7 — 自己上一轮加的缓存指纹不够

上一轮给 `bc_demos.npz` 加的指纹只哈希磁盘上的 db.M 和 db.C。本轮的风、科氏、舵、K_T、对称性、导数重标**全都不改那个文件**——缓存会被静默复用。

改为**行为指纹**：固定海况、固定且同时激励推力和舵的指令，跑 3 秒，用末状态做指纹。物理一变它就变，改注释不会变。

### E8 — 船型通用化（`hydro/hull.py`）

`Hull` 携带一次运行需要的四样东西：几何（解析形式或网格文件）、质量（排水量、重心、回转半径）、附体（舵、桨）、水动力（BEM 数据库，按参数哈希缓存在磁盘）。`NonlinearVessel(..., hull=h)` 从它取剖面表、附体几何和对称性声明；不传 `hull` 时行为与原来完全一样。

导入自己的船只需要：

```python
Hull(name="ourboat", L=..., B=..., T=..., mesh_kind="file",
     mesh_path="ourboat.stl", displacement=..., z_cog=...,
     k_roll=..., x_rud=..., rudder_area=...)
```

然后 `hull.check()`（排水量与型线是否闭合、回转半径是给定的还是来自几何、舵叶是否在水下）→ `hull.database()`（Capytaine 的惯量被替换为 A30 修正过的值，对称性按声明施加）。

已验证：同一船体换成真实回转半径后，SS5 下**横摇 rms 2.86° → 4.47°（+56%）**，艏部加速度 rms 0.229 → 0.238 g，航速 4.51 → 4.48 m/s。质量分布主要影响横摇，这正是物理上该有的样子。第一版这里报的是“航速 4.06 → 3.36 m/s、加速度 0.160 → 0.188 g”——那是在错误坐标系下测的（E11），航速的下降是伪振荡造成的，已作废。

### E9 — 外部方法与数据的实际状态

**已为本船计算的（闭式方法）**：

| 方法 | 结果 |
|---|---|
| Whicker-Fehlner 升力线斜率 | 本舵 2.15–3.33 /rad；原 3.5 高出 5%，已改为计算值 |
| Wagner 楔形入水精确解 | PILE_UP = (π/2)² 与精确解一致，误差 0 |
| Pierson-Moskowitz | 风速 11.4 m/s，已用于风载荷 |

**需要先取得数据才能做的**（不凭记忆复现系数）：Wageningen B 系列、Molland & Turnock、Blendermann / Isherwood、Ikeda、Holtrop-Mennen、Journée / ITTC Wigley、SIMMAN、Gothenburg / Tokyo 研讨会、Zhao, Faltinsen & Aarsnes（第 21 届海军水动力学研讨会，MARINTEK 的楔形与艏外飘剖面跌落试验，数据只有图；此前误引为 Zhao & Faltinsen 1993，那篇是数值方法）、实船自由衰减。每一项能标定什么，见 `studies/external_methods.py` 第 3 节。

### E10 — 本轮作废的产物

- 所有 PPO / BC 权重及其 VecNormalize 统计量：被控对象变了，需要重训
- `bc_demos.npz`：行为指纹会自动判为过期并重采
- 报告里引用的回转半径、横摇固有周期、艏部加速度数值

MPC 在最终的被控对象上仍能正常转向：SS5 中 60 s，艏向误差 −0.5°，横向偏差 −0.39 m；降阶模型对艏部加速度的预测在整个 12 s 视界内都可用（E11）。

### E11 — 按错误的坐标系"补全"了科氏项 ⚠️ 本轮最重要的一条

D1 说"刚体科氏力 6 个分量只写了 2 个"，E1 据此补上了完整的体坐标系科氏矩阵。**这是错的。**

- `kinematics()` 把升沉、横摇、纵摇速率直接积分成位移（ż = w，θ̇ = q），只有水平速度随艏向旋转。也就是说本模型的坐标系**只在艏摇上旋转**：水平面是操纵性坐标系，振荡模态是耐波性坐标系——标准的混合做法。
- 在这样的坐标系里，科氏项只来自艏摇旋转 ω = (0, 0, r)。刚体部分**恰好就是原来那两项** −mvr、mur。原来的代码是对的，我的"缺失项清单"是错的。
- 加上 w、p、q 那几项后：升沉方程多出约 6 kN rms 的伪力（−m·u·q）；**静水稳态回转时船以约 17°/s rms 的横摇角速度摇晃**（w/p/q rms 0.27 m/s、0.30 rad/s、0.14 rad/s，正确答案是 0）；MPC 降阶模型对艏部加速度的预测相关性从 0.9 掉到 0.4（`model_horizon` 显示"可用时长 0.0 s"）。
- 斜对称检验和 Euler 方程检验**全程通过**——它们验证的是代数，不是这套代数属于哪个坐标系。

> **一项检验通过，只说明它检验的那件事是对的。Euler 方程验证了"这是体坐标系的科氏矩阵"，没有验证"这个模型是体坐标系的"。**

发现它，是因为改完后重跑了一个看起来不相关的检验（`model_horizon`），数字掉了；再去看静水回转中的 w/p/q，发现不是 0。

修复：`coriolis()` 先把 w、p、q 置零再构造。

| | 体坐标系（错） | 艏摇坐标系（对） |
|---|---|---|
| 静水回转 w/p/q rms | 0.27 / 0.30 / 0.14 | 0 / 0 / 0 |
| 降阶模型艏部加速度相关性（1.5 / 3.5 / 7.5 s） | 0.44 / 0.48 / 0.48 | 0.88 / 0.91 / 0.87 |
| 艏部加速度可预测时长 | 0.0 s | 12.0 s |
| 35° 回转半径 | 1.83 L（伪振荡把 Munk 力矩的均值削掉一半） | 1.79 L（Nr' 重新辨识后） |

连带的修正：

- **Nr' 0.0040 → 0.0060**。0.0040 是对着一艘在静水中摇晃的船辨识出来的。在正确坐标系下重辨识：35 组里 13 组可行，回转半径 1.09–2.19 L（2.0 倍），中心 Yv' = 0.025、Nr' = 0.0060 → 1.79 L。
- **Munk 力矩在正确坐标系下更强**：单独加上它，35° 回转半径 1.98 → **0.66 L**（漂角 24.9°），是 N_r' 翻倍才把它拉回来的。所以现在的回转圆本质上是"势流 Munk 力矩"和"占位的 N_r'"之间的平衡，两者都不确定：Munk 用 A(ω→0) 还会再翻倍；与之相抵的升力部分 N_v 没有建模。
- **E8 的质量分布对比作废重测**，见 E8。
- 那部分被误以为缺失的物理——升沉、纵摇随航速的耦合——是真实存在的，但它属于**随航速变化的水动力系数**（切片理论或带航速的格林函数），`LIMITATIONS` 里本来就列着"零航速水动力系数"。它现在作为缺失项列在 `studies/missing_terms.py` 的 Tier A 里，而体坐标系那几项列在"撤回"之下。

## F. 换船型做实验时暴露的方法缺陷

这一轮每个实验都换成数据对应的船型：KVLCC2（1:68 横摇衰减，SSPA）、KCS（T2015，波浪中的升沉/纵摇/横摇）、Kawahara 2009（简化 Ikeda 的原始方法算例），外加五个船型的整链鲁棒性测试。F1–F7 是方法层面的问题，F8 是实验结果，F9 是鲁棒性测试。

### F1 — CAD 导入：坐标轴和重复面片，都能通过原来的体积检验 ⚠️

- KVLCC2 的 IGES 里 x 指向**船艉**（原点在艉垂线 AP），z 指向**下方**（龙骨在 z = 0，船体在负 z 一侧）。第一次按 z 向上导入，平底被当成甲板，找到的吃水比公布值浅 7.5%。
- 平底在文件里**出现两次**（面片 1、2 完全相同）。原来的闭合检验只用 n_x、n_y 两种散度形式，而平底上 n_x = n_y = 0：这两种形式恰好看不见平底，缺了或多了都能通过。
- 修复：调用者声明 `forward=`、`up=`（和单位一样，不猜）；闭合检验用三种形式（n_z 形式能看见水平面）；删除落在其它面片上的面片（`redundant_patches`）；方盒自检覆盖"双层底、缺底、倒置、一个面翻向"四种情况。
- 与公布值核对（最细网格；SIMMAN 与 SSPA）：吃水 +0.08%（随网格加密从 +0.33% 收敛，粗网格的弦线切掉了 2.4 m 的舭部圆弧），水线长 0.00%，C_B −0.08%，LCB 3.505% vs 3.48%，KM −0.11%。
- **只有 LCB 的符号能分辨船艏和船艉**：体积、吃水、GM 都与方向无关。x 轴的方向就是被它发现的。
- 湿面积比 SIMMAN 表多 1.76%，但与 Alexandersson 用同一型线得到的 5.981 m²（模型尺度，折算 27,656 m²）只差 0.05%。差异在表，不在网格。

### F2 — 面片取向：逐片投票在 653 片的 KCS 上失败

- 原方法：每个面片独立地看"法向是否背离中轴线"投票。KVLCC2 的 19 片可以；KCS 有约 50 片投票太弱，其中一些被翻错，浸没体积的两种形式相差 2.3%。
- 修复：沿面片之间的**公共边**传播取向（相邻两片的三角形在公共边上必须反向），这是纯拓扑，不做几何判断；每个连通集只问一次几何问题。结果 KCS 的整个壳体本来就是一致的，没有一片需要翻。
- "丢弃小碎片"不能默认开启。KCS 的面片缝合成一个壳（舵、桨毂帽是独立的连通集），KVLCC2 的面片**没有缝合**，按 10% 规则会把它的艏、艉面片当碎片丢掉，船体就破了。改为调用者指定 `min_component`，连通集结构总是打印出来。

### F3 — 频域参考解的时间约定是反的，M3 门的相位比较也是反的，两处互相抵消 ⚠️

- Capytaine 的复幅值用 e^{−iωt}：它的入射波是 exp(+ik·x)，只有配 e^{−iωt} 才朝 β 方向传播；它自己的阻抗写成 −ω²(M+A) − iωB + C。`rao_frequency_domain` 用了 +iωB。
- M3 门（`sim/verify_rao.py`）用"哪个时间约定能让时域相位对上这个参考解"来选符号，于是选了 e^{+iωt}。它的相位比较也按另一约定读：拟合 y = a·cos(ωt + φ) 时，应该比较 φ 与 −arg ξ。两个错误互相抵消，门一直通过。
- 为什么一直没发现：Wigley 前后对称，升沉–纵摇耦合 A35 只有约 1e-7，两种约定下的**幅值完全相同**（M4 门只比幅值），相位在 1.5–3 rad/s 却差 30–168°。
- 换成真实船型就不一样：KCS 2.10 工况 C3 的升沉，正确约定 0.692，原约定 0.490（−29%）。
- NonlinearVessel 不受影响：它的波浪场按 e^{i(k·x−ωt)} 施加 F，正是 Capytaine 的约定。
- 修复后 M3 选 e^{−iωt}（相位误差 0.2°，错误约定 135.4°），全部通过。判定频率从 1 rad/s 改为 2.5 rad/s：1 rad/s 时升沉滞后不到 1°，两种约定看起来都对。

### F4 — 波浪激励不随艏向转动 ⚠️ 本轮最重要的一条

- 传递函数只在初始化时按**大地坐标系**的浪向插值一次，相当于永远假设艏向 ψ = 0；条带站位沿大地 x 轴排；桨、舵处的水面高度也按大地 x 取。船转向时只有遭遇频率变了（因为相位按船的移动位置取），激励在各自由度之间的**分配**完全不变。
- 数值演示（同一个相对横浪，区别只是转的是船还是浪）：

| | 升沉/波幅 | 横摇 | 纵摇 |
|---|---|---|---|
| 浪转 90°，船不动 | 1.003 | 0.59° | 0.00° |
| 船转 90°，浪不动（修复前） | 0.919 | **0.00°** | **0.54°**（= 迎浪的答案） |
| 船转 90°，浪不动（修复后） | 1.002 | 0.59° | 0.02° |

- 这对本项目是要害。RL 研究的前提是"艏向对遭遇频率的控制权是推力的 15 倍"，而转向对激励本身的影响此前**完全缺失**，只剩遭遇频率。
- 一并修复：浪向折叠到 [0, π] 时，横荡、横摇、艏摇没有反号（左右镜像下这三个自由度变号）。短峰波里从一侧来的浪，被当成从另一侧来的在推。
- 修复：`HeadingTransfer` 每步按相对浪向 θ − ψ 插值；站位、桨、舵的位置按 ψ 旋转。ψ = 0 时结果不变（M4、物理审计 28/28 通过）。

### F5 — 波浪增阻多算了 (ω_e/ω)²

- Gerritsma-Beukelman 方法：一个遭遇周期内船体辐射的能量，等于增阻在"波形相对船移动的距离 λ/|cos β|"上做的功，所以 R_aw = (k|cos β|/ω_e)·∫b′⟨V_z²⟩dx，其中 **k 是波本身的波数 2π/λ = ω²/g**。代码用了 ω_e²/g，在本船 4.5 m/s、Tp 9.7 s 迎浪下高估 1.69 倍；遭遇频率也固定用迎浪公式。
- 修复后 M4 的 SS5 失速从 8.5% 变为 8.9%。同一次改动里横向符号和艏向也变了，而且这是单种子、20 s 的平均，这个数说明不了增阻变了多少；增阻本身按公式降为原来的 (ω/ω_e)²。

### F6 — 缓存与数据库：换船型时静默答错

- `Hull.database()` 的问题：
  - 默认只算迎浪一个方向，任何艏向下都没有横向激励；
  - 只存 L，B、T 回落到 2.5 和 0.8；
  - **先存盘后修正**惯量和对称性，按路径加载会得到 L = 1 m 和未修正的惯量；
  - 缓存键不含频率和浪向网格。

  修复：默认 13 个浪向（0–π），存 L、B、T、ρ，修正后再存盘，缓存键包含网格。
- 波浪激励的缓存键只含频率网格。同长度的两个船型共享网格，第二条船会拿到第一条船的力（审计中实测）。修复：键中加入传递函数内容。
- 辐射记忆缓存按 `id(db)` 取键，而对象回收后 id 会被复用。修复：按内容哈希。

### F7 — 换船型跑不通或静默出错的其余项（审计清单节选）

已修：

- 舵的通气阈值 h_full 是绝对的 0.10 m，静水断言在吃水 < 0.465 m 时直接崩溃（2 m 双体、6 m 船模、1:68 KVLCC2 都会中）；而 `Hull.check` 用的是 0.05 m，放过了随后会崩溃的船。修复：h_full = 0.18 × 舵展（USV 仍是 0.10 m），两处用同一判据。
- `MeshSections` 假设网格原点在船中（站位取 ±L/2）。KVLCC2 的原点在 AP，站位只盖住后半条船。修复：站位按网格实际范围取，龙骨取网格最深点；CAD 船的原点移到浮心（平浮时即重心）。
- 绝对占位参数（粘性阻尼、推进器上下限与时滞、舵速、风力臂、增阻平均时间）不管船多大都不变，油轮上也装着 12 kN 的推进器。修复：按 λ = L/10 做 Froude 缩放（10 m 时与原值完全相同），并打印警告：这些不是那条船的数据。

当时未修（已在 G1 修复）。这些属于 USV 控制栈本身，换船需要重新设计，不是改几个常数：

- MPC：推力标度 12000 N，视界 12 s；
- 降阶模型辨识：8000 N 阶跃，0.5 m 释放高度；
- RL 环境的奖励归一化：砰击按 20 kN、横向偏差按 10 m；
- env 的甲板上浪阈值 0.55 m；
- PPO 的帧堆叠 3 s；
- 各门测的绝对阈值：航速 0.5–20 m/s、通气深度、回转观察 60 s；
- 默认数据库 `hydro_wigley_10m.npz`。

### F8 — 实验结果（以测量为锚）

**KVLCC2 横摇衰减**（SSPA 1:68；预测完全不拟合衰减数据，`studies/exp_kvlcc2_roll_prediction.py`）

| 量 | 预测 | 测量 |
|---|---|---|
| 固有周期（模型） | 2.543 s（横荡–横摇–艏摇耦合） | 2.545 s |
| 附加惯量 A44/I44 | 0.168（BEM） | — |
| ζ_eq(5°)，0 kn | 0.0077 | 0.0049 / 0.0052 |
| ζ_eq(5°)，15.5 kn | 0.0129 | 0.0145 |

- 周期吻合只在给定 kxx 的前提下成立。kxx = 0.40 B 是目标值，kxx 每差 1%，由周期反推的 A44 就差约 14%。
- 0 kn 阻尼高估 33–65%，并随幅值增大：简化 Ikeda 的涡旋分量斜率是测量的 1.5–2.5 倍。C_M = 0.998 超出公式范围（0.90–0.99）；按 0.99 计，涡旋分量少 21%，5° 时仍高估 29%；按 0.98 计只高估 7%。高估的部分集中在涡旋回归对 C_M 最陡的那一段，而这条船正好落在范围外面。所以"对这条船不适用"比"公式错了"更准确，但这只是一个数据点，不能反过来去标定公式。
- Ikeda 的波浪阻尼回归给出 0.0039，BEM 给出 0.0006（仅横摇 0.0011）。测量的线性部分 ζ1 = 0.0012–0.0030，其中还包含约 0.0015 的摩擦，装不下回归值。对这条船，**回归被数据否定，BEM 与数据相容**。
- 15.5 kn：升力分量占预测的 53%；Ikeda 的波浪阻尼航速修正在 Ω ≈ 0.24，因子 4.7，恰好落在经验公式的峰上。预测低估 6–16%。

**KCS，T2015，Fr 0.26**（零航速 BEM + 遭遇频率，即本项目耐波性模型的线性极限；`studies/exp_kcs_t2015_seakeeping.py`）

| λ/L | 升沉 模型/测量 | 纵摇 模型/测量 |
|---|---|---|
| 0.65 | 0.55 | 1.65 |
| 0.85 | 0.43 | 1.43 |
| 1.15（近共振） | 0.77（相位差 39°） | 0.96 |
| 1.37 | 1.09 | 1.05 |
| 1.95 | 1.04 | 1.14 |

- 长波（λ/L ≥ 1.37）误差 4–14%，共振区和短波差到 2 倍。kyy 变化 ±0.01 L 只影响 ≤ 8%，所以这是方法本身（零航速近似）的误差，不是惯量假设造成的。
- 斜浪横摇（2.11）差 0.27–2.5 倍。但 45°、90° 工况下，测量横摇的高次谐波达到一次谐波的 40–80%，线性对比本身就不干净；kxx 在 0.36–0.44 B 之间变化，横摇就变 −37% 到 +33%。**横摇的结论：不确定。**

**简化 Ikeda（Kawahara 2009）的实现核对**（`hydro/ikeda.py`）

- 208 个系数与 ITTC 表逐一比对，论文与 ITTC 只在 2 处不同：−17.102 / −17.109，31.4113508 / 31.41135。
- ITTC 规程与原文在 6 处不一致：涡旋公式的 x2 应为 C_B、摩擦公式应为 C_B、Q5 求和上限、tanh 的括号，以及上面两个数值。另有 ITTC 自己标注的原文勘误。tanh 括号那一处可以用数学判定：只有 tanh(20(Ω−0.3)) 在零航速时给出 1。
- 与原始 Ikeda 方法的计算结果对比（论文图 1 的符号，从 PDF 矢量路径中精确提取）：涡旋 1–9%，舭龙骨 1–4%，波浪 20–43%（RMS）。对数取自然对数，log10 会差 32–46 倍——这是用数据判定的，不是猜的。

### F9 — 五个船型的整链鲁棒性测试（`studies/exp_hull_robustness.py`）

链条：Hull → 网格 → 剖面 → BEM 库（40 个频率 × 7 个浪向）→ NonlinearVessel → 静水 20 s → Froude 缩放的 SS5（所有船 Hs/L = 0.325）、30% 推力、60 s（均按 √λ 缩放时间）。

| 船型 | λ | 静水升沉/T | 静水纵摇 | 有限 | u/u_ref | 最大升沉/T | 纵摇 | 横摇 | 艏摇 | 砰击 |
|---|---|---|---|---|---|---|---|---|---|---|
| Wigley USV 10 m | 1.0 | 9e-17 | 0.000° | 是 | 0.79 | 3.34 | 14.1° | 13.9° | 9.0° | 2 |
| 双体 2 m | 0.2 | −3e-16 | 0.000° | 是 | 0.73 | 5.04 | 11.3° | 4.0° | 5.2° | 1 |
| 方驳 20 m | 2.0 | 3e-16 | 0.000° | 是 | 0.56 | 2.05 | 14.8° | 22.5° | 251° | 1 |
| KVLCC2 1:68 | 0.471 | 1.3e-5 | −0.001° | 是 | 0.78 | 4.41 | 12.6° | 9.3° | 9.8° | 6 |
| KCS 1:37.9 | 0.607 | 5.4e-5 | −0.004° | 是 | 0.75 | 5.52 | 13.3° | 20.3° | 3.1° | 6 |

- 修复前：双体、KVLCC2 1:68、KCS 这三条在构造 NonlinearVessel 时就会因舵的断言崩溃（吃水都 < 0.465 m）；CAD 船型没有进入 `Hull` 的路径；站位按 ±L/2 取；数据库只有迎浪。
- 现在五条都能跑通，静水中保持平浮。CAD 船有 1e-5 T 量级的漂移，来自条带体积与 BEM 网格体积之间的微小差别。
- **这只证明链条能跑、能闭合，不证明结果是对的**：
  - 占位参数都是 USV 按 Froude 缩放的，每次都会打印警告。
  - 海况按 Hs/L 不变来缩放，对 KVLCC2 相当于实船遇到 Hs ≈ 100 m，所以升沉会达到吃水的 2–5.5 倍。这是故意的压力测试，不是工况。
  - 方驳转了 251°：舵固定在 0（没有自动舵），操纵导数 Yv′、Nr′ 用的是商船范围，而方驳本来就没有航向稳定性。这个数没有物理意义，只说明艏向大幅变化时链条不会崩，而且激励会跟着转（F4）。
  - 砰击判据是"最后一个站位露出 −T"，隐含 Wigley 的等吃水假设（F7 未修项），对有艏柱倾斜或球鼻艏的船只是近似。
- 耗时：KCS 709 s（全船网格，不能用对称，另加盖板消除不规则频率），其余 57–206 s。

## G. 换船型的修复：控制栈、统一入口与通用验收

这一轮的目标：换成自己的船时，从几何到训练脚本整条链都跟着船走，不再藏着 10 m USV 的常数；F 节实验暴露的问题，用同样的测量回头验证。

### G1 — 控制栈里的 USV 绝对常数（F7 当时未修的部分）

| 位置 | 原来 | 现在 |
|---|---|---|
| 降阶模型辨识 | 在另建的一条 Wigley 上辨识（Wigley 剖面、B 2.5、T 0.8），横荡系数却读自传入的被控对象：两条船拼成一个模型 | 在被控对象自己的静水副本上辨识（`with_sea`） |
| 辨识输入 | 8000 N 阶跃、0.5 m 释放、240 s 等绝对值 | 推力上限的固定比例、0.625 T、时长 × √λ |
| MPC | 推力 × 12000 N、舵 × 35°、站位 ±L/2 | 被控对象自己的推力上限和舵角；站位从船艉到船艏（取自网格） |
| MPC 出水判据 | 船中吃水 | 艏部站位自己的龙骨深度 |
| 自动舵 | ω_n = 0.30 rad/s，舵限 35° | 0.30/√λ，被控对象的舵限 |
| 闭环指标 | 砰击阈值 0.8 m、上浪阈值 0.55 m、横向偏差除以 10 m | 艏部吃水、船体干舷、除以船长 |
| RL 环境 | 观测是原始 m/s、rad/s；砰击按 20 kN 归一 | 按 λ 做 Froude 缩放（USV 本身数值不变）；砰击 20 kN × λ³ |
| 训练脚本 | 各自按路径加载 `hydro_wigley_10m.npz`，U_REF 4.5，T_END 120 s | `--hull NAME`；设计航速，时长和预视 × √λ |

- 对 10 m USV，每一项都恰好等于旧常数。新旧路径逐位比较：降阶模型的全部参数、一个闭环回合的全部指标、RL 环境 40 步的观测和奖励，差值都是 0。
- 海况（Hs、Tp）保持绝对值：海况是任务，不是船的属性。

### G2 — 统一入口：`hydro/hulls.py` 与 `sim/config.py`

- 船按名字注册（`hydro/hulls.py`，含给你自己船的模板 `our_boat`）；所有脚本经 `config.load(name)`、`config.plant_for(db, sea, hull)` 取船，不再各自按路径建。
- 新增保护：数据库属于别的船、却没有传 `hull=` 时，`NonlinearVessel` 直接报错。原来会静默变成"那条船主尺度的 Wigley 加上 USV 的附体"。
- 旧的 Wigley 数据库文件（150 个频率）继续使用，历史结果可以复现。
- CAD 导入结果按内容缓存（`mesh_<name>_<hash>.npz`）：一次导入要一分钟 gmsh，而 RL 的每个工作进程都会重建船体。
- M3、M4 门测按角度找迎浪下标，不再写死下标 12（只在 0–π 的 13 个浪向网格上碰巧是 π）。

### G3 — 实验：KCS 的短波误差是不是缺了航速项？（`studies/exp_kcs_stf_speed.py`）

F8 里 KCS（Fr 0.26，迎浪）的短波升沉只有测量的 0.43–0.55，纵摇是 1.43–1.65 倍。本项目的水动力系数都是零航速的，最直接的怀疑就是缺了航速项。

- 做法：在同一套零航速 3D BEM 系数上加 Salvesen–Tuck–Faltinsen（1970）的航速修正。辐射部分改 A35、A53、B35、B53、A55、B55；绕射部分给纵摇力矩加上 (U/iω_e)·F3 的绕射分量。与同一批 T2015 测量比较。
- 没有包含艉封板（transom）项：它需要艉封板处的剖面附加质量，3D BEM 给不出。

| λ/L | 升沉：零航速 | 加辐射项 | 再加绕射项 | 纵摇：零航速 | 加辐射项 | 再加绕射项 |
|---|---|---|---|---|---|---|
| 0.65 | 0.55 | 0.54 | 0.57 | 1.65 | 1.44 | 1.68 |
| 0.85 | 0.43 | 0.78 | 0.76 | 1.43 | 1.42 | 1.39 |
| 1.15 | 0.77 | 1.24 | 1.29 | 0.96 | 0.96 | 0.99 |
| 1.37 | 1.09 | 1.34 | 1.45 | 1.05 | 0.87 | 0.92 |
| 1.95 | 1.04 | 1.06 | 1.17 | 1.14 | 0.93 | 1.04 |

- 五个工况的平均 |ln(模型/测量)|（0 表示完全吻合，0.69 表示差 2 倍）：
  - 升沉：0.366（零航速）→ 0.284（加辐射项）→ 0.326（再加绕射项）；
  - 纵摇：0.214 → 0.193 → 0.196。
- 以测量为锚能说的：
  - 短波纵摇的高估（1.4–1.7 倍）**不是**缺航速项造成的，加上以后几乎不变。
  - 升沉在 λ/L 0.85 从 0.43 改善到 0.78。但在共振附近（λ/L 1.15、1.37）从 0.77、1.09 变成 1.24、1.34，相位误差则从 −39°、−25° 缩小到 −25°、−8°。共振峰由阻尼决定，而不含艉封板项时，STF 不给升沉阻尼 B33 任何航速修正。
  - 平均误差略降，但原来最准的长波区（误差 4–14%）变差了。证据不足以把它放进被控对象，所以**不改**：零航速系数继续列在 LIMITATIONS 里。要真正解决，需要带航速的格林函数，或含艉封板项的切片理论，这一轮没有做。

### G4 — 实验：被控对象本身对 KVLCC2 横摇衰减（`studies/exp_kvlcc2_plant_decay.py`）

F8 是在频域里预测横摇阻尼，这里跑的是时域被控对象本身：从每次测量的第一个极值处释放，对输出拟合与测量相同的衰减方程，再比较 5° 时的等效阻尼 ζ_eq。三种横摇阻尼来源：

| 0 kn（两次重复）/ 15.5 kn | 占位值（USV 按 Froude 缩放） | 简化 Ikeda（超出适用范围） | 衰减试验输入 |
|---|---|---|---|
| 修复前 | 3.33, 3.13 / 1.14 | 1.67, 1.58 / 0.75 | 0.85, 0.86 / 0.97 |
| 修复后 | 3.33, 3.13 / 1.15 | 1.68, 1.58 / 0.76 | **0.95, 0.96 / 0.97** |

（数值是模型/测量；周期 2.565 s，测量 2.545 s，偏 +0.8%。）

找到并修了两处：

1. **衰减试验的换算多扣了兴波阻尼。** 衰减试验测的是横摇模态的总阻尼，被控对象自己已有 BEM 的兴波部分，所以只需补上余下部分。原来扣掉的是单独横摇的 B44，但船自由漂浮时横摇会经横荡、艏摇向外辐射，模态的兴波阻尼大约只有 B44 的一半（F8）。多扣的那一部分，就是 0.85 倍的来源。
   - 修复：扣除"把横荡、艏摇凝聚掉之后的横摇阻抗"里的兴波阻尼。被约束（拖曳）时仍按单独 B44 扣。
   - 修复后 0 kn 两次都是 0.95–0.96。这一行不是预测，而是"输入了衰减试验，被控对象能不能原样还回来"的一致性检验。剩下的 5% 来自二次项的换算和周期差。
2. **"auto" 在超出适用范围时退回占位值，而占位值更差。** KVLCC2 只因 C_M 超出范围。0 kn 时占位值是测量的 3.1–3.3 倍，Ikeda 外推是 1.6–1.7 倍；15.5 kn 时占位值在 5° 恰好接近（1.15），但它是纯二次的，而测量是线性的（k2 = 0），形状就不对。
   - 修复："auto" 改用 Ikeda，并在报告里标出超出范围的参数。多体船（`multihull=True`）仍用占位值，因为 Ikeda 是单体船剖面的方法；Wigley 显式声明用它自己的占位值，结果不变。
   - 这只是一个船型的一个数据点，结论只能说到这里：对自己的船，最有价值的输入仍然是一次横摇衰减试验。

### G5 — 验收门测在别的船型上找到的被控对象缺陷 ⚠️ 本轮最重要的一条

新的验收门测（`studies/hull_acceptance.py`）第一次在六个船型上跑：Wigley 和双体船 8/8，方驳 3/8，KVLCC2 7/8，KCS 两个工况各 7/8。逐个定位下来是七处真缺陷，**全都在 Wigley 上看不见**：

1. **约束自由度（captive）的实现是错的。**
   - 原来：先用整个质量矩阵求出全部加速度，再把被约束自由度的加速度清零。
   - 问题：自由度之间有附加质量耦合（如纵荡–纵摇）时，本该被拖车吸收的纵荡力仍经由耦合去驱动升沉和纵摇，等于凭空多了一个能量源。被拖着的方驳在小波浪里发散，180 s 时升沉达到 1e39 m。
   - 修复：自由自由度只用它们自己那一块质量矩阵 M_ff 求解。
2. **静水恢复矩阵作用在艏向角上。**
   - BEM 的恢复矩阵在重心不在浮心正上方时，带一个横摇–艏摇项 −ρgV(x_B − x_G)。被控对象把整个矩阵乘到状态上，而艏向 ψ 是大地坐标系下的绝对角度，于是船转了多少度，就多出多少横摇力矩。
   - 数值核对：KCS 2.11 在同一个相对浪向下，"浪转"与"船转"两种设置的物体坐标系波浪力完全相同，只有横摇力矩差 1.67 N·m，正好等于该项 −1.594 乘以 60°。KCS 的 GM 只有 7 mm，这一项单独就压出 17° 横摇；在回转圈里它会一直增长，直到倾覆。
   - 修复：纵荡、横荡、艏摇三列清零，它们是位置，不是离开平衡位置的位移。Wigley 的 LCG = LCB，这一项本来就是 0。
3. **零推力指令处不连续。**
   - 指令的定义是"设计航速下的推力"，所以趋近于零的指令对应"设计航速下推力为零时的转速"。这个转速在任何更低的航速下都在推船。
   - USV：推力状态衰减到 1e-6 N 时，静止状态下仍有 2057 N 推力。
   - B 系列更糟：转速趋近于零时进速系数 J 到了 1e5，远在回归的拟合范围之外，给一条 1 吨的 KVLCC2 船模 7 万牛。
   - 推力状态按指数衰减，永远不会恰好为零，所以这不是边界情况，而是每一次"关推力"都会发生。
   - 修复：推力低于死区时，轴转速随指令线性降到零；B 系列在零推力进速系数 J0 之后推力取零（不描述反转、制动象限）。死区以上完全不变。
4. **辐射记忆被喂进了整个前进速度。**
   - 记忆项是零航速下对振荡运动的响应模型，只在波浪频率上受 BEM 约束，低频段是放任的。
   - 把船的前进速度连同加速过程一起送进去，方驳的纵摇记忆力矩达到 6.3e7 N·m，是恢复力矩的 30 倍，83 s 时翻船。
   - 对照实验：只去掉记忆项的纵荡输入，方驳就稳定了。
   - 修复：纵荡输入改为相对于缓慢变化的平均航速（时间常数 20 s × √λ 的低通）的偏差。波浪频率的纵荡照常通过；匀速拖曳的船模不再有纵荡辐射，这本来就是对的。
5. **辨识实验复用同一个对象，推进器延迟线里残留着上一个实验的指令。**
   - 衰减实验一开始，就被推力阶跃留下的 8 kN 指令推了一把。Wigley 在所谓"自由衰减"中爬到约 2 m/s；在 KVLCC2 上，这一推再叠加第 3 条，引发了发散。
   - 修复：每个辨识实验都用一条新的静水副本。
6. **辐射记忆的积分在某些拟合上数值不稳定。**
   - 修完前五条后，方驳仍在 D–H 五项上发散，而且发散时刻与横摇阻尼用哪种模型无关（都在 148.14 s，状态完全相同）。线性检查 C 能通过，而 C 恰好用的是更小的步长。
   - 核对：整个状态（包括记忆状态）用 Heun 法（二阶龙格–库塔）积分，而它对轻阻尼的高频极点不是无条件稳定的。方驳在默认步长 0.071 s 下，80 个极点中有 16 个的单步放大系数大于 1（最大 1.0126，极点 −0.031 + 8.36i）；双体船 98 个中有 6 个（1.0042）。Wigley、KVLCC2、KCS 的全部小于 1。
   - 表现是先缓慢增长、然后突然爆炸，所以跑得短的检查看不出来。步长减半后方驳的 D、E 都稳定，原因由此确认。
   - 修复：记忆是线性子系统，每一步用矩阵指数精确推进（一步之内速度按线性变化）。对任何稳定的拟合、任何步长都稳定，其余状态仍用 Heun 法。
7. **推力上限的占位值与设计航速不一致。**
   - 按 Froude 缩放 USV 的 12 kN，隐含"设计航速的 Froude 数与 USV 相同"。KVLCC2 的设计 Froude 数是 0.14（USV 是 0.45），全推力能跑到设计航速的 4.2 倍，降阶模型也就在 3.4 m/s 上辨识。
   - 修复：推力占位值取"USV 的推力裕度（阻力的 2.1 倍）× 本船设计航速下的阻力占位值"。设计航速按 Froude 缩放时，与原值完全相同。

8. **辨识的周期估计器不稳健。**
   - 升沉、纵摇的固有频率原来用"整段记录所有过零点间隔的平均"来估计，衰减完之后的数值尾巴也算了进去。
   - 修上面几条的过程中，Wigley 的升沉频率先后辨识成 3.27、3.58、3.26 rad/s，而这些改动都不涉及它的升沉物理。
   - 修复：从船最终静止的平衡位置量起，只用振幅还在释放量 2% 以上那一段的过零点，过零时刻在步内插值，再由有阻尼周期换算回无阻尼频率。
   - 稳健性核对：步长减半时，频率变化 0.2–0.6%，阻尼比变化 0.6–2.7%，时间常数变化不超过 1.1%（Wigley 与 KVLCC2）。

对 Wigley 的影响：
- 新旧两条路径仍逐位一致：被控对象、降阶模型、闭环回合、RL 环境都相同。
- 被控对象本身只在第 3、4、6 条涉及的情形下改变：推力接近零、前进速度变化、记忆积分。
- 降阶模型：升沉固有频率 3.27 → 3.29 rad/s，纵摇 3.52 → 3.62 rad/s，艏摇时间常数 3.3 → 2.65 s。艏摇时间常数的变化来自第 4、5 条：旧值是在"衰减中被残余推力推着走、记忆项还在响应起步冲击"的条件下辨识出来的。
- 门测：M3、M4、物理审计 28/28、操纵门测全部通过；螺旋桨实验结果不变（5.4%，两次测量之间 9.6%）。
- `studies.model_horizon` 的结论不变：MPC 内部模型对艏部加速度可用到 12 s，航向 2 s，艏摇角速度 1 s。

### G6 — 通用验收门测：六个船型的最终结果（`studies/hull_acceptance.py`）

| 船型 | C 线性极限（升沉/纵摇最大误差） | D 艏向不变 | E 全推力航速 / 设计航速 | F 回转半径 / L | G 缩放 SS5 最大横摇 | H 闭环：平均航速 / 目标，航向 rms | 通过 |
|---|---|---|---|---|---|---|---|
| Wigley USV 10 m | 0.26% / 0.15% | 0 | 1.32 | 1.79 | 5.6° | 0.97，1.1° | 8/8 |
| 双体船 2 m | 0.47% / 0.22% | 2e-16 | 1.35 | 1.12 | 5.4° | 0.98，1.6° | 8/8 |
| 方驳 20 m | 0.64% / 0.64% | 3e-16 | 1.30 | 0.57 | 18.9° | 0.88，4.3° | 7/8 |
| KVLCC2 1:68 | 0.21% / 0.20% | 2e-16 | 1.40 | 1.05 | 12.9° | 0.89，5.2° | 8/8 |
| KCS 2.10 | 0.15% / 0.14% | 0 | 1.38 | 1.73 | 45.7° | 0.98，1.8° | 8/8 |
| KCS 2.11 | 0.16% / 0.18% | 4e-16 | 1.38 | 1.73 | 44.0° | 0.98，1.9° | 8/8 |

- A（完整性）、B（静水平衡）六个船型全部通过。
- 方驳唯一没过的是 F：回正舵 85 s（按 √λ 缩放）后，艏摇角速度仍有转舵时的 53%，即航向不稳定。长宽比 2.5、没有鳍的方驳本来就会这样；它的 Y_v′、N_r′ 也是从商船回归（Clarke 1983）外推的，C_B = 1 远在其范围之外。这一项失败说明门测在起作用：对自己的船，若这里失败，说明航向要靠自动舵来稳定，操纵导数必须实测。
- KCS 在缩放 SS5 下横摇达 44–46°，仍在"不倾覆（57°）"之内。T2015 的装载 GM 只有 7 mm / 16 mm（GM/B 约 2%），而横摇又取决于占位的 kxx（取自几何）和 Ikeda 阻尼，这个数没有经过验证。
- 这套门测证明的是：模拟在这条船上是它自己方程的一个自洽解，整条链（从几何到 RL 环境）都跟着这条船走。它**不能**证明模拟与真船吻合，那只有试验能做到；每个船型还在用哪些占位值，都列在它自己的报告里。

