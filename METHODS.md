# 方法总览：每一段代码用了什么理论、算出什么量

按数据流顺序。每一节写清楚：**输入 → 用什么理论 → 输出什么量 → 怎么验证的**。

---

## 离线：几何与水动力

### `hydro/geometry.py` — 参数化船型与网格

| 项 | 内容 |
|---|---|
| **理论** | Wigley 数学船型的解析定义 |
| **公式** | $y(x,z) = \frac{B}{2}\left(1-(\frac{2x}{L})^2\right)\left(1-(\frac{z}{T})^2\right)$ |
| **算出** | 2562 个顶点、2400 个四边形面片的湿表面网格 |
| **同时给出解析真值** | $V = \frac{4}{9}LBT$（$C_b = 4/9$）、$A_{wp} = \frac{2}{3}LB$、$C_{33} = \rho g A_{wp}$、$C_{55} = \rho g BL^3/30$ |
| **法向定向** | 散度定理 $V=\frac{1}{3}\oint \mathbf{r}\cdot\mathbf{n}\,dS$，为负则翻转面片绕向 |
| **验证** | 二阶收敛到解析值：40×14 网格体积 −0.14%，80×28 为 −0.04% |

球体（`hemisphere_body`）只用于验码，因为它有解析解。

### `hydro/bem.py` — 边界元求解（**唯一真正解流体的地方**）

| 项 | 内容 |
|---|---|
| **理论** | 线性势流 + 边界元法。假设无粘、无旋、不可压 ⇒ 存在速度势 $\varphi$，$\nabla^2\varphi=0$ |
| **边界条件** | 自由面 $-\omega^2\varphi + g\partial_z\varphi = 0$；深水衰减；无穷远辐射条件；物面条件（见下） |
| **求解方式** | 用满足自由面条件的**格林函数**把船体表面离散成源分布，解 $N\times N$ 复数线性方程组求源强度，再积分压力 |
| **两类问题** | **辐射**：$\partial_n\varphi_R^{(j)} = n_j$（静水中船以单位速度运动）<br>**绕射**：$\partial_n\varphi_D = -\partial_n\varphi_I$（船不动，抵消入射波穿透） |
| **算出** | $A(\omega)$ 附加质量 (150,6,6)、$B(\omega)$ 辐射阻尼 (150,6,6)、$F_{FK}$、$F_{diff}$ (150,13,6) 复数、$C$ 静水刚度、$M$ 刚体惯量 |
| **不规则频率** | 内部域共振产生的伪峰。加自由面盖板消除（ω=4.43 处的伪影消失） |
| **验证** | 球体两个独立解析极限：$A'(ka\to0)=0.8479$ vs Hulme 0.831（+2.0%）；$A'(ka\to\infty)=0.4995$ vs 整球镜像解 0.5（−0.1%）。互易性 $\|A-A^T\|/\max\|A\| = 6.9\times10^{-4}$ |

**成本**：$O(N^3)$ 每频率，150 频率约 15 分钟。之后再也不碰流体——545 KB 的数据库就是这条船的全部水动力特性。

### `hydro/retardation.py` — 频域转时域

| 项 | 内容 |
|---|---|
| **理论** | **Cummins (1962)** 方程 + **Ogilvie (1964)** 关系 |
| **要解决的问题** | $A(\omega)$、$B(\omega)$ 依赖频率 ⇒ 时域里等价于力依赖**运动历史**（记忆效应）⇒ 卷积 ⇒ 不是 ODE ⇒ MPC 用不了 |
| **公式** | $K(t) = \frac{2}{\pi}\int_0^\infty B(\omega)\cos\omega t\,d\omega$ （余弦变换）<br>$A_\infty = A(\omega) + \frac{1}{\omega}\int_0^\infty K(t)\sin\omega t\,dt$ |
| **算出** | 延迟函数 $K(t)$（1500 点，dt=0.02 s）、无穷频率附加质量 $A_\infty$ |
| **数值处理** | 加**升余弦渐变窗**——积分上限是 ∞ 但网格有限，硬截断会产生 Gibbs 振铃散布到整条 $K(t)$ |
| **状态空间实现** | **ERA（特征系统实现算法）**：由 $K$ 构造 Hankel 矩阵 → SVD → 奇异值谱的"悬崖"给出真实阶数 → $A_d = \Sigma^{-1/2}U^TH_1V\Sigma^{-1/2}$ → $A_c = \log(A_d)/\Delta t$ |
| **物理剪枝** | 剪掉时间常数远长于 $K$ 实测记忆的伪极点——它们在拟合截断残余，且会毁掉 $A(\omega)$ 的低频端 |
| **算出** | $(A_r, B_r, C_r)$ 使 $C_re^{A_rt}B_r \approx K(t)$，10 阶 |
| **验证** | 合成系统（已知真值）：极点恢复 0.177%，$A_\infty$ +0.06%。真实船型：往返 28/28 通过，波频带内 $B$ 误差 0.05–0.19% |
| **产出的物理量** | **水动力记忆 = 2.2–2.9 s** |

> **踩过的坑**：$H(i\omega)=\int Ke^{-i\omega t}dt$ ⇒ $\mathrm{Im}[H]=-\int K\sin dt$ ⇒ $A = A_\infty \mathbf{+} \mathrm{Im}[H]/\omega$。我原来写的减号，而合成测试**用同一个错误约定构造真值**，两边抵消查不出来。

### `hydro/freesurface.py` — 自由面重建

| 项 | 内容 |
|---|---|
| **理论** | 同一套 BEM 解；动力学自由面条件 $\eta = -\frac{1}{g}\partial_t\varphi\big|_{z=0}$ |
| **关键点** | 源强度求出后，势不只能在**物面**积分得力，也能在**自由面场点**求值 |
| **合成** | $\eta_{\text{扰动}}(\omega) = \eta_D(\omega) + \sum_j \xi_j(\omega)\eta_R(\omega,j)$，$\xi_j$ 是复数 RAO |
| **算出** | 16 频率 × 61×41 场点的复数传递函数表 |
| **产出的物理量** | SS5 谱峰处扰动 **0.2%** 波幅；真实海况中峰值 **0.042 m** vs 波浪 ±2.2 m ≈ **2%** |
| **结论** | 预视链路"只用入射波"的假设成立 |

### `hydro/kelvin.py` — 定常 Kelvin 波系（艏波 + V 形尾流）

上面那张表里的扰动是**非定常**的。船真正最显眼的那道波——艏波和身后的 V 形
尾流——它一个都不含，因为整套 BEM 是**零航速**解出来的。这个模块把它补上。

| 项 | 内容 |
|---|---|
| **理论** | Michell 薄船理论（thin-ship）；自由波必须相对船静止 ⇒ $U\cos\theta=\sqrt{g/k}$ |
| **由此严格导出** | $k(\theta)=k_0\sec^2\theta$，$k_0=g/U^2$。**整个几何**都由这一条定死：19.47° 半角、横波波长 $2\pi U^2/g$、发散波、以及深水里所有自由波都在船**后面** |
| **谱形（导出）** | 中纵剖面上的源强 $\propto \mathrm{d}h/\mathrm{d}x$；Wigley 船型下 $\mathrm{d}h/\mathrm{d}x=(B/2)(-8x/L^2)(1-(z/T)^2)$ **可分离**，$x$、$z$ 两个积分都有闭式解 |
| **幅值权重** | 取自 Michell 的**兴波阻力**被积函数 $R_w\propto\int|I|^2\sec^3\theta\,\mathrm{d}\theta$ ⇒ 振幅 $\propto |I|\sec^{3/2}\theta$。这样按 $\cos^{3/2}$ 衰减、可收敛；直接用 $\sec^3$ 根本不衰减 |
| **唯一标定量** | 绝对幅值用**能量**定：尾流带走 $R_wU$ 瓦，$R_w$ 取本项目自己那条阻力律的一个比例（`RW_FRACTION = 0.45`）。这是全模块唯一的假设 |
| **算出** | 3.5–6.5 m/s 下峰值 **0.43–0.61 m**，横波波长 7.8 / 13.6 / 19.4 / 27.1 m |
| **验收（6/6 通过）** | 横波波长与 $2\pi U^2/g$ 相符（<8%，实测**完全吻合**）、半角 20.8–22.3° vs 19.47°、方位角积分 RMS 收敛 <5%、且该残差随网格加密**下降**（7.1%→2.9%）、峰值落在 0.1–1.5 m、尾流 RMS 随航速单调上升 |

**几何是物理，高度是标定**：波形和波长是导出的；总高度通过能量平衡锚在模型
自己的阻力上，量级对，不是精确值。它**只用于可视化，不回灌到受力**——兴波阻
力已经含在拟合的二次阻力里，加两遍就是重复计。

**标定的代价（明说）**：强行令 $R_w\propto U^2$，抹平了兴波阻力随 Froude 数
真实存在的**驼峰与波谷**。波形对，航速趋势被磨平了。

**三个坑，全是"尺子"而不是"物理"**（见 `DEFECTS.md` A17–A19）：
数值积分在大方位角处失效、按数零点测波长偏短、以及用单像素最大值测收敛。

---

## 时域：船体模型

### `sim/sections.py` — 剖面几何与非线性 FK

| 项 | 内容 |
|---|---|
| **理论** | 条带法 + 散度定理精确面积分 |
| **为什么需要** | SS5 下 $H_s/T_{吃水} \approx 4$，船体经常整体出水再落下，线性理论（在**固定**平均湿表面上积分）不够 |
| **解析剖面积** | $-T<d\le0$: $A = 2y_0[d - \frac{d^3}{3T^2} + \frac{2T}{3}]$；$d>0$ 加墙侧延伸 |
| **任意网格的精确算法** | 取 $\mathbf{F}=(0,z-d)$ 则 $\nabla\cdot\mathbf{F}=1$，于是 $A(d)=\oint(z-d)n_z\,ds$。**在 $z=d$ 上被积函数为零 ⇒ 封口线段贡献为零 ⇒ 不用拼轮廓**<br>$A(d) = -\sum(y_b-y_a)\left[\frac{z_a+z_b}{2}-d\right]$<br>$M(d) = -\sum(y_b-y_a)\left[\frac{z_a^2+z_az_b+z_b^2}{6}-\frac{d^2}{2}\right]$ |
| **算出** | 每站位的浸水剖面积、形心深度（用于 Smith 因子） |
| **验证** | 解析 / BEM / 数值刚度三方一致：$C_{33}$ 误差 0.002%，$C_{55}$ 0.010%，$C_{35}\sim10^{-13}$。网格版收敛到 +0.018%，舯剖面形心 −0.29999 vs 理论 $-3T/8=-0.30000$ |

### `sim/wavefield.py` + `step0_preview_spec.py` — 海浪

| 项 | 内容 |
|---|---|
| **理论** | 线性波叠加 + JONSWAP 谱 + $\cos^{2s}$ 方向扩展 + 深水色散 |
| **公式** | $\eta(x,y,t)=\sum_j a_j\cos(k_j(x\cos\theta_j+y\sin\theta_j)-\omega_j t+\phi_j)$<br>$a_j=\sqrt{2S(\omega)D(\theta)\Delta\omega\Delta\theta}$，$k=\omega^2/g$ |
| **Smith 效应** | 动压随深度按 $e^{kz}$ 衰减，**逐分量**施加（短波衰减远快于长波） |
| **算出** | 任意 $(x,y,t)$ 的波面高程、垂向速度、深度衰减后的等效高程 |
| **验证** | 合成 Hs 3.26 vs 目标 3.25；谱峰 0.670 vs 0.648 rad/s；谱形与目标 JONSWAP 吻合 |
| **已知局限** | 严格高斯——真实 SS5 偏度 +0.1~0.3，极端波峰被低估 |

### `sim/cummins.py` — 线性 6-DOF 时域核

| 项 | 内容 |
|---|---|
| **方程** | $(M+A_\infty)\dot\nu + \mu(t) + C\eta = \tau_{exc}(t)$，$\dot\eta=\nu$ |
| **记忆项** | 由 M2 的状态空间给出：$\dot{x}_r = A_rx_r+B_r\nu$，$\mu = C_rx_r$ |
| **组装** | 36 个 (i,j) 对中只有 **10 个非零**（左右对称使纵向组与横向组不耦合），拼成块对角 $A_{big}(96{\times}96)$ |
| **积分** | RK4 |
| **验证** | **时域 RAO 对频域解析解**（两条完全独立的计算路径）：垂荡最差 0.28%，纵摇 0.10%，相位 0.2°。长波极限垂荡 RAO → 1.0000 |
| **时间约定** | **实测判定**为 $e^{+i\omega t}$（$e^{-i\omega t}$ 给出 7.8° 相位误差），不是假设 |

### `sim/vessel.py` — 非线性 6-DOF 船体

| 力 | 理论 | 说明 |
|---|---|---|
| **线性激励 + 回复** | BEM 传递函数，逐分量 $\mathrm{Re}[\sum F(\omega_j,\beta_j)a_je^{i\psi_j}]$ | 广谱不被塌缩到单一频率 |
| **非线性 FK 增量** | $\tau = \tau_{\text{BEM线性}} + (\tau_{\text{条带非线性}} - \tau_{\text{条带线性化}})$ | **小幅时括号项→0，精确退化为 BEM**；条带理论的误差（λ/L→1 时增长）只作用在修正量上。线性极限误差 **0.21%**（直接替代是 10.4%） |
| **辐射记忆** | $\mu = C_{big}x_r$ | |
| **波浪增阻** | **Gerritsma-Beukelman** 辐射能量法 $R_{aw}=\frac{k}{\omega_e}\int b'_{33}\langle V_z^{*2}\rangle dx$ | 剖面阻尼按 $\text{beam}^2$ 分布；均方用指数滑动平均（公式本身是均值不是瞬时力） |
| **粘性阻尼** | 二次阻尼 $-D\nu\|\nu\|$ | **占位值**，势流对横摇几乎不给阻尼 |
| **科氏力** | 刚体 $C(\nu)\nu$ | 保留了纵荡×艏摇→横荡的耦合 |
| **前进速度** | 船在**自己的移动位置**查询波场 | 遭遇频率是**结果**不是参数 |
| **砰击** | **Ochi 判据**：艏部出水 + 再入水相对速度 > $0.093\sqrt{gL}$ | 事件检测（边缘触发），不是逐时间步计数 |
| **积分** | Heun（执行器在积分之外单独推进，因为带速率限和延迟） | |

### `sim/actuators.py` — 执行器

| 项 | 内容 |
|---|---|
| **为什么单列** | 文献里 sim-to-real 的**第一失败源**是执行器，不是水动力 |
| **推进器链路** | 死区 → 饱和 → 传输延迟 → 速率限 → 一阶滞后 |
| **通气** | 推力损失随桨轴浸深单调下降，1.5 倍桨半径以上全湿 |
| **舵** | 升力 $\propto u^2$，含失速软化；同时产生**侧向力和艏摇力矩** |
| **状态** | 纯函数 `effective()` 可在导数里调；有状态的 `advance()` 每控制步只调一次（否则延迟线被污染） |

---

## 控制与学习

### `control/reduced.py` — MPC 的降阶模型

| 项 | 内容 |
|---|---|
| **为什么** | plant 有 110 个状态，MPPI 每步要滚 192×24 次，滚不动；MPC 需要的是**正确的时间尺度**，不是正确的水动力 |
| **结构** | 纵荡一阶 + 二次阻力；垂荡/纵摇二阶振子；艏摇 Nomoto 一阶 |
| **关键区别** | 每个系数都从**已验证的 plant 辨识**出来，不是拍的 |
| **辨识方法** | 和真船一样的三个试验：推力阶跃、自由衰减、舵角阶跃 |
| **算出** | $\tau_u=5.9$ s、$k_{drag}$、$\omega_{n,heave}=3.58$、$\zeta=0.227$、$K_{nomoto}=-1.50$、$T_r=3.35$ s |

### `control/mpc.py` — MPPI 采样型 MPC

| 项 | 内容 |
|---|---|
| **为什么用采样而非梯度** | 砰击是离散事件（代价非光滑）；预视以时变扰动预报的形式进入，不是约束 |
| **节点参数化** | 7 个节点而非 24 个时间步——24 维空间几百个样本探索不动，7 维可以，而且产生的是平滑的"减速-加速"协调计划 |
| **代价** | $\sum(w_{acc}(a_{bow}/g)^2 + w_{slam}\cdot\text{slam} + w_{spd}(\frac{u_{ref}-u}{u_{ref}})^2 + w_{track}(y/L)^2 + w_{hdg}\psi^2)dt$ + 控制平滑项 |
| **更新** | softmax 加权 $w_k=e^{-(c_k-c_{\min})/\lambda}$ |
| **热启动** | 计划**展开到时域 → 移一步 → 重采样回节点**（直接移节点索引会前进 ~4 步） |
| **预视** | 一等参数：`t_preview` 和噪声。超出预视时长给零（零均值波面的统计期望） |

### `sim/env.py` — 闭环与自动舵

| 项 | 内容 |
|---|---|
| **架构** | **MPC 管推力，经典自动舵管航向** |
| **为什么分开** | 短峰浪给船艏摇力矩，而降阶模型既无横荡状态也无横向波浪激励——MPC 控舵时艏向直接发散（RMS 476°） |
| **自动舵理论** | 对辨识出的 Nomoto 模型 $\dot r=(K\delta-r)/T$ 配极点：<br>$a=\omega_n^2T/K$，$b=(2\zeta\omega_nT-1)/K$，$\delta=-(a\psi_{err}+br)$ |
| **算出的增益** | $\|a\|\approx0.14$——我原先手拍的 2.2/4.5 **高了 15 倍**，舵在追每个浪，舵阻力拖慢航速，而舵力 ∝ u²，正反馈发散 |
| **积分** | 条件积分（抗饱和）；$\omega_n=0.30$ 远低于遭遇频率 0.9，守平均航向而不追每个浪 |
| **结果** | 四个种子艏向 RMS **0.7–0.9°**，横漂 1.0–2.8 m |

### `learn/tune.py` — 权重调优

| 项 | 内容 |
|---|---|
| **动作空间** | **MPC 的 7 个权重**，不是舵 |
| **为什么** | 7 个有界数字用 10²–10³ 个 episode 就能调完——**装得进一次海试**。端到端神经网络策略要 10⁷ 步，真船上跑不完 |
| **算法** | 紧凑 CMA-ES（7 维不值得引依赖，写出来也便于复现） |
| **目标函数** | **操作方的**目标，不是 MPC 自己的代价——用后者只是教控制器同意自己 |
| **泛化检验** | 在未见过的波浪实现上复验 |

---

## 可视化

### `studies/visualise.py` — matplotlib 3D
姿态和吃水取自**真实闭环运行**，不是摆的。水面按局部高度裁掉水下面片，水线才落在该在的位置。

### `studies/export_viewer.py` + WebGL 查看器

**浏览器不播录像，它重算波面**——和物理用的是同一个谱：

$$\eta(x,y,t)=\sum_{j=1}^{64} a_j\cos(k_{x,j}x + k_{y,j}y - \omega_j t+\phi_j)$$

在**顶点着色器**里求和，法向由同一个和式**解析求导**（不是差分）。所以拖时间轴是在新时刻重新求值，水面和船姿永远自洽。

**分量打包进 vec4**：GLSL 的 `float arr[N]` 在多数驱动上每个元素占满一个 vec4 槽，6 个 float 数组共 298 槽会超出 WebGL 保证的 128（WebGL2 是 256），着色器链接失败 ⇒ 整页黑屏。打包后 64 分量占 128 槽。

**能重算的和不能重算的**：波场是解析的，浏览器能算；**船的轨迹是 110 状态非线性求解的结果，只能录**（Python 里也只有 29 倍实时）。

**Kelvin 尾流：烘焙成一张贴图**。它的相位积分要几千个方位角才不混叠（见
`theta_samples_needed`），那是一分钟的 numpy，不是一帧的预算。但它在**船坐标系
里是定常的**，所以只需算一次，之后每个顶点一次纹理采样就够了。

航速用**相似性**处理，而不是烘一叠贴图：相位 $k_0\sec^2\theta(x\cos\theta+y\sin\theta)$
中令 $X=xg/U^2$ 后 $U$ 完全消失 ⇒ 波形在 $x/U^2$ 下**严格自相似**，着色器按
$(u/U_0)^2$ 拉伸即可，幅值同因子（因 $R_w\propto U^2$）。**不**自相似的是谱
$A(\theta)$——它仍通过 $k_0L$ 看见船长；在船实际保持的 $U_0\pm20\%$ 内，这只轻微
改变横波与发散波的配比。

采样是**顶点着色器**里做的：这是位移，不是着色。另取两个 tap 求斜率，尾流才
能吃到光；否则它只是一个看不见的位移。

---

---

## 贯穿全项目的方法论

1. **每个里程碑都有可证伪的验收门**，过不了不往下走
2. **优先用不依赖外部数据的检验**：解析极限、互易性、往返一致性、量纲极限
3. **两条独立路径互验**：时域 vs 频域、解析 vs 网格、条带 vs BEM
4. **自洽的往返测试查不出约定错误**——必须有携带正确约定的外部真值
5. **指标本身也会骗人**：误差可能测的是渐变窗、是单点采样噪声、是一个退化站位
6. **占位参数必须显式标注**（见 `DEFECTS.md` C 节），绝不能当结论引用
